# One Piece Fighter — 项目规范

网页版海贼王 1v1 格斗游戏（玩法参考拳皇）。总体规划见 `docs/PLAN.md`，本文件只写约束和约定。

## 技术栈

- TypeScript（strict）+ Phaser 3 + Vite
- 测试：Vitest，覆盖 core、AI、键位映射和动画取帧等纯逻辑；Phaser 场景另做浏览器实机验收
- 包管理：npm，锁文件 `package-lock.json` 必须提交
- GitHub同步与公开游玩：2026-09-14用户明确授权将仓库公开并生成可直接玩的访问链接。`main` 保存源码、测试、设计和来源记录，继续忽略正式素材；`gh-pages` 单独保存已验收构建的运行文件白名单（含实际采用的人物、舞台、技能和声音），通过 GitHub Pages 公开。此授权覆盖旧条款中禁止正式运行素材入 Git 和禁止公开地址的部分，仅适用于该部署包；不发布原图、失败稿、源音频包、浏览器资料、本地归档或验收录像，不改游戏战斗数值。
- 公开部署目录：`.local-releases/公开部署-0914/<时间>/` 保存复制前清单、`site/` 白名单运行包、复制后哈希和公网验收结果，保留各次结果不覆盖旧回退。`scripts/packagePages.mjs` 负责可复现打包，不自动推送；先检查再推送 `gh-pages`，Pages 只读取该分支根目录，不新增自定义 Actions 工作流。公开根网址默认完整动漫模式，保留显式旧版参数。
- 0914 打包中实际遇到 C 盘 ENOSPC，本次发布暂存根显式指定为 `D:/one-piece-fighter-pages-0914/`，根目录先放结构说明；仍按时间保存清单、site 和验收记录，不搬移或删除原项目和失败包。本轮新建的临时验证构建移至该目录的 `build-check/`，复制前清单和移动后哈希留档。

## 目录约定

```
docs/            规划与设计文档（PLAN.md 为总纲；design/ 帧数据与平衡记录；research/ 调研笔记）
public/assets/   运行时静态资源（characters/ stages/ ui/ audio/），含版权素材不进 Git
src/core/        纯格斗逻辑：固定步长、确定性、零 Phaser 依赖
src/characters/  角色数据（def.ts 基础参数 + moves.ts 招式表 + animations.ts），一个角色一个目录
src/render/      Phaser 层：scenes/ hud/ fx/ FighterView DebugOverlay
src/input/       键盘 / 手柄 → InputFrame
src/ai/          CPU 对手与训练木桩（只产生 InputFrame，不直接改 core 状态）
src/audio/       音频封装
scripts/         开发辅助脚本：TS 用 `npx vite-node scripts/<name>.ts`（可复用 @core 等别名），图像处理用 Python + Pillow；产物写入 public/assets（placeholder* 提交，atlas.* 由 assets-src 再生成）
assets-src/      正式素材来源（设定图等），frames.json 由 export-frames.ts 生成
tests/           Vitest 测试，按 src 中的纯逻辑模块分目录
```

## 精灵图集约定

- 每角色一个 TexturePacker JSON Hash 图集：`public/assets/characters/<id>/atlas.png` + `atlas.json`（正式素材，不进 Git）；缺失时回退到 `placeholder.png/json`（脚本生成，可进 Git）；两者都缺时渲染层画色块。
- 帧名 `<id>/<anim>/<n>`，n 从 0 起。`<anim>` 是 StateId（`idle`、`walk_fwd`、`hit_air`…）或招式 id（`st_a`、`sp_gatling`…）。
- 默认招式帧号为 FrameData.sprite；接入已验收的连续精灵时，可在角色动画表声明与逻辑帧同步的视觉序列，但起手、命中、收招分界必须与 FrameData 对齐，不能为播放素材改变招式时长、伤害或判定。非招式状态的帧数、fps、是否循环由 `src/characters/<id>/animations.ts` 声明。
- 帧尺寸可以不同：每帧在 JSON 里给 `pivot: {x, y}`（归一化，脚底中心通常 y = 1），Phaser 会按 pivot 设置 origin；没有 pivot 的帧按 origin 0.5 / 1 处理（占位图集）。
- 自制设定图来源放 `assets-src/characters/<id>/`，旧设定图切图规则保留在 `scripts/cut_concept_art.py`。当前 `npm run gen:atlas` 使用 `scripts/build_sprite_atlas.py` 与下述显式精灵清单；版权精灵源与图集只放本地忽略目录。
- 2026-09-11 用户已验收路飞《Gigant Battle! 2》连续精灵四动作样板，并明确同意掌机像素风、两年后造型后继续接入。连续精灵源只放本地 `public/assets/characters/<id>/source-sheet.png`，裁切与来源记录放 `scripts/sprite_manifest.json`，通过显式角色配置生成正式图集。两角色原 `sheet.jpg` / `moves.jpg` 保留作为回溯基线。禁止按 `pixel/` 等目录是否存在自动覆盖正式姿势；来源或姿势不合法时停止生成。未取得并检查同风格素材的角色继续保留已有图集，不伪称完成。
- 程序绘制的像素样板只写系统临时目录，不接入 `gen:atlas`。已有 `assets-src/characters/*/pixel/` 为未采用的历史试验，保留但不使用。
- 更换角色美术前，先展示一个角色的站立、走路、出拳、受击的正常速度样板，并由用户验收。人物轮廓、辨识度与身体完整性不得低于当前基线；不得把“像素风”“帧数齐全”或测试通过当作美术验收。

## 硬约束

### 0913 完整游戏改造（当前授权，覆盖旧过程门槛）

- 用户已批准 `docs/游戏改造计划0913.md`，连续完成两角色、一舞台、全部现有招式的人机、本地双人、训练和完整比赛。优先级为能完整玩、画面精美统一、攻防有趣。保留 Phaser 与固定 60 Hz 战斗底座。
- 中间样板、动作和视听由代理自行检查、修复并继续，取消旧条款中等待用户批准小样再扩产的依赖。用户主观听感与复玩验收集中在完整版本交付后；不能把内部检查或样板能运行称为整款完成。
- 全部新增 AI 原画、补画仍须核实 GPT Image 2.5 官方网页入口，保留完整生成证据；入口不可用只暂停依赖它的制作，其他工作继续。禁止用旧像素角色或同一静帧的重复命名冒充完整新动作。
- 0913 用户补充：明确提示词的常规生图优先使用非 Pro 文字模式，不占用 GPT 6 Pro 做额外文字推理；深度规划与复审可用 Pro。此偏好不改变 GPT Image 2.5 图像型号硬要求。每次生成分别记录文字模式与图像入口依据，已经提交的生成不重做。
- 新版资源异常阻止创建战斗，提供重试与主动进入旧版。完整动作覆盖检查通过后才解除训练与能力限制。表现配置必须贯穿菜单、选人、比赛、设置、结算和重赛。
- 完整动漫模式（`art=anime&scope=full`）将海军本部 `backdrop/floor` 两份舞台资源与两角色 FX 图集作为必需交付包；加载层返回结构化结果，校验实际使用的技能主体、接触及辅助帧，下载/解码失败、非法或越界图集、缺必需帧均由 Preload 明确报错并支持重新载入，不静默换成程序背景或粒子。旧版及内部样板保留可选表现资源行为；动漫舞台仍沿用已批准的同源整幅构图，不因资源校验改绘制方式。
- 原图打包的 `textureDensity` 与显示尺寸分离，坐标和连接点同步换算。逻辑世界保持 480×270；人物缩放为 `(renderScale / 2) / textureDensity`。默认新版采用双倍密度、1920×1080 画布，性能档为双倍密度、960×540，不换画风。单页不超过 4096，按动作组分页。
- 抓投允许对指定原画显式声明 `foregroundPolygon`，将同一人物的近侧前臂/手套分为前景层。只分配已处理原图像素，不重画或变形；身体与前景必须不重叠、不丢像素，复合后逐字节等于原人物。夹层仅在真实投技且当前姿势显式分层时启用，使受投者位于身体与近臂之间；经对照确认的释放姿可延续到投技结束，受击、退出与切回普通帧须恢复正常显示。层资源同样纳入分页、哈希和缺帧校验，不增加动作覆盖数。
- 本轮新增文档放 `docs/`，采用主题加 0913 命名；来源证据放 `docs/assets/`。快照和清单放 `.local-releases/改造基线-0913/`，构建放 `.local-releases/candidate-0913/`，内部验收媒体和结果放 `.local-releases/改造验收-0913/`，均保持本地忽略；先清单、复制、校验，不删除既有文件。临时实验仍放系统临时目录。
- 旧 `dist/` 与 0912 候选保留完整回退。2026-09-14 当前 `index-Crbo9vzy.js` 已通过完整流程、正式通用语音和独立 Edge 900 秒持续交战内部检查（详见 `docs/High收尾实施与验收0913.md`）。保存并核验本次完整交付快照后，`开始游戏.cmd` 调用 `playCandidate.mjs --release candidate-0913 --full` 打开完整动漫标题；`开始旧版游戏.cmd` 调用 `playLocal.mjs` 打开原 `dist/`。入口仅启动现有本地构建，不安装、不构建、不发布。真人听感、画面喜好与复玩评价仍单独验收。
- 0913构建使用 `node scripts/playCandidate.mjs --release candidate-0913`，默认本机4177端口，保留0912入口默认4176。不加 `--full` 仍打开内部动作样板训练；显式增加 `--full` 仅支持0913，打开完整动漫标题。后续改动不得只凭完整参数或素材覆盖通过跳过内部验收；交付快照位于 `.local-releases/改造基线-0913/完整交付-0913/`，先保存清单、复制并逐项校验，不删除或覆盖旧回退。
- 声音保留用户设置，真实区分静音、零音量、未解锁、暂停、下载/解码/启动失败；提供开启、试听、失败重试及主动恢复默认。没有实际听辨确认的日语候选不计入正式覆盖。
- 0914 用户已确认两份试听串烧的角色声线。人工选用的通用语音映射与文件哈希单独保存为 `scripts/voice_manifest.json`，运行时清单由 `scripts/apply_voice_manifest.py` 校验后合入；音频重制脚本亦调用同一校验，避免重制清掉已确认声音。声线确认、游戏事件用途选择与准确台词确认分开记录，不将未确认招名接成专属喊招。
- 美术替换期间冻结伤害、范围、时长。可复现的输入或攻防错误可修复；平衡变化须单独记录前后证据。四项工程检查、实际画面与试听、完整比赛和 15 分钟桌面浏览器运行分别记结果。

### 0912 整体品质升级（用户已批准实施）

- 用户在完成网页 Pro 咨询及合成计划后明确要求“现在开始全力执行计划”。当前获准执行 `docs/整体品质升级计划0912.md`，包括人物小样、生图、素材处理、代码/声音接入与验证；先完成可操控双机制小样，再按计划的集中样板验收门槛扩产，不重复询问已定方向。
- 新方向为精美、统一的动漫 2D 横版格斗：人物、动作、技能、舞台与界面共同设计；仍是两角色、一舞台、1v1，保留既有格斗底座。旧“保留像素人物不动”的假设不再约束新计划。
- GPT Image 2.5 的硬性要求扩展到所有新 AI 人物原画、动作关键姿势、补帧与重绘；继续使用可核对型号的官方网页入口，不以其他图像或视频模型替代。规划咨询与图像生成的型号记录分开，网页只显示 `6 Pro` 时不写成已证实后台 Astra 型号。
- 新人物路线必须先通过同屏、正常速度、可操控样板验收，再批量制作；不以静图好看或测试通过替代动画与手感验收。已验收的旧人物、素材和构建作为回溯基线保留。
- 规划上下文、网页原始建议、合成计划放 `docs/`，主题加 MMDD 命名；来源证据继续放 `docs/assets/`。不保存凭证与无关聊天。
- 已完成的合成规划见 `docs/整体品质升级计划0912.md`，网页咨询原始记录见 `docs/网页Pro规划建议0912.md`；二者均为规划成果，不表示新人物或新视听已完成。
- `.local-releases/<主题-MMDD>/` 保存本地可回退构建、工作区快照和验收媒体，不进 Git；先复制并校验清单，再生成新的候选构建。临时检查文件仍放系统临时目录，不删除旧文件。
- 用户手动上传的网页Pro复审包放`.local-releases/网页Pro复审0912/`，包括上传说明、可复制问题、`package/`白名单材料和主题加MMDD的ZIP；来源、文件长度与SHA256记录在包内清单。只复制当前任务所需的代码、图片、音视频与说明，不含`.git`、`node_modules`、凭证、浏览器配置、无关聊天或全部历史归档；不自动上传。用户截图注明实际证明范围，受控故障截图不能当作用户当前画面。打包脚本放系统临时目录，不改候选构建；后续清理仍须按删除红线处理。
- 新人物原始下载继续放 `public/assets/generated/source/<character>-<action>-MMDD[-candidate].png`；透明候选帧及候选图集放 `public/assets/characters/<id>/anime/`，保持旧正式 atlas 不动。`scripts/anime_manifest.json` 保存显式来源、裁切/根点/连接点/时序，`scripts/build_anime_atlas.py` 仅做确定性后处理与打包，不能绘制代替模型原画。
- 0913 同坐标局部原画回贴可在单帧声明 `sourcePatches: [{"source": "已声明来源key", "rect": [x,y,w,h]}]`，每项只允许这两个字段。原图与补图均须通过 `load_sources` 的 GPT Image 2.5 官方网页来源及文件哈希校验，且整张 PNG 尺寸完全相同；矩形为原图坐标的正面积整数区域，不越界、不重复、不重叠。先把补图该区域的原始 RGBA 像素原坐标替换到原图副本，再按原帧裁切、透明处理和统一纹理密度打包；不做透明混合、不改根点、连接点或原身体比例，不使用补图独立比例、不缩放或变形补片，不与局部几何变形混用。打包报告保留每块来源、矩形、前后局部像素哈希，以及补片并集外按行序的保护区前后 RGBA 哈希；本次路飞火箭仅回贴 `[475,550,213,174]` 头颈区域，原身体来源不变。注释和审查结论放帧 `note` 或交付报告；哈希相等只证明像素保护，不代替实际动作与接缝验收。
- 0913 已视认绿底污染的局部外沿可显式声明 `transparency.despillAlphaFeather: {regions: [[x,y,w,h]], minGreenExcess: 16, fullGreenExcess: 64}`。区域使用裁切内整数坐标并绑定原图 SHA256、审查说明；只对原像素绿色超过红蓝最大值的部分渐减已有 alpha，不改 RGB、不碰非绿色原像素、不用于整人轮廓删除。减量以去色前原像素为依据，不能把绿转黄后再靠“绿色计数零”称作视觉合格；同裁切前后图、保护像素哈希与正常显示大小检查均须保留。本次仅赤犬双掌已视认的火星外沿与披风旁窄带采用，硬剪造成白披风破洞的试验不采用。
- 0913 素材覆盖审计必须同时追踪帧的原图与 `sourcePatches` 补图；补图用于正式动画、未引用帧或 NOT_BUILT 候选时分别统计，不因补图不是主来源而漏记，也不把候选补图算成正式使用。每个正式帧的原图和全部补图均须通过来源、型号与提示词哈希检查，任一补图未知或核验失败即不能计作有效覆盖。
- 0913 界面人物从已核实母版单独裁切，保持原始像素密度，不能放大战斗缩图充当高清界面图。显式裁切与来源放 `scripts/ui_portrait_manifest.json`，`scripts/build_ui_portraits.py` 核验原图、型号和提示词哈希后生成 `public/assets/characters/<id>/anime/ui-0913.png` 与 `src/render/anime/uiArtManifest.json`（仅尺寸、坐标、内容哈希等元数据）。全身与头像共享同一张透明原尺寸纹理，不添加战斗动作；加载校验失败沿用明确错误和重试，不回退到旧像素图。裁切复核放 `docs/assets/界面人物裁切0913.md`；胜利界面仍用真实 win 动作。
- 路飞橡胶段允许显式、离线的局部几何校准：在已核实 Image 2.5 原图上指定直段起点、腕点、压缩比例与小幅抬腕角度，仅重采样已有像素；身体/脚点/肩/袖口保持不变，拳形尺寸不缩放，不新画肢体或填色补洞。参数、保护区像素一致性、连接处透明度和真实判定对照必须留证。此规则不授权整人拉伸或无依据的通用动作补帧。
- 大喷火首稿与一次定向返工已解决同手连接但仍未满足射程。针对该样板允许一次同原理的离线熔岩段校准试验：只延展原图中已成形、与身体连续的熔岩前臂横段，固定肩/红袖/普通身体/脚和非能力手；末端实体拳只平移、不缩放，不复制拳、不绘制补洞。变换前后保护区哈希一致、拳形保持、连接不断、绿底无残留，且与真实攻击走廊对照后才可接入。只增加透明画布空间不构成生图；不改任何战斗数据。试验如产生明显拉花或解剖问题则不采用，不据此授权通用人物变形。方法与失败原图见`docs/assets/大喷火局部校准方法0912.md`。
- `src/render/anime/` 保存候选动画配置和样板的输入边界；`?art=anime&mode=training` 是限定动作样板，未制作状态明确关闭，不当作整款游戏已完成。样板阶段构建输出到 `.local-releases/candidate-0912/`，默认 `dist/` 保持旧可玩版；来源与实际结果记录在 `docs/assets/` 和 `docs/整体品质实施记录0912.md`。

### 0911 视听升级（历史授权，0912 已暂停扩展）

- 所有新增 AI 生成或重绘的背景、技能特效和演出图片必须使用 GPT Image 2.5，通过 ChatGPT 官方网页版绘图入口操作；每次生成前核对页面型号标识。入口或型号无法确认时暂停对应生图，不换模型；音频、代码和验证可继续。
- `docs/assets/` 保存素材来源、网页型号依据、生成会话、完整提示词、时间与文件 SHA256；使用主题加 MMDD 命名。不得保存账号凭证或无关聊天记录。
- AI 原始下载图放 `public/assets/generated/source/`；处理后的舞台层放 `public/assets/stages/marineford/`，技能贴图和图集放 `public/assets/fx/`。这些本地图片不进 Git，来源记录和可复现裁切/打包脚本可提交。失败候选保留原图，不覆盖已采用素材。
- 录制音效放 `public/assets/audio/sfx/`、日语原声放 `public/assets/audio/voice/<character>/`、原始包放 `public/assets/audio/sources/`；全部本地忽略。原有根目录自制 WAV 保留为回退。逐条记录来源与角色/事件映射，未取得语音不标完成。
- 保留两角色精灵和战斗规则；新增投射物结束原因等纯数据事件仅供表现消费，不改变伤害、攻击范围、出招时长。特效主体使用图像，代码负责运动/粒子/光效与缓存，不以普通光球替代正式技能。
- 验收包括实际画面与试听、完整比赛、十五分钟连续运行及四项工程检查。当前批准方案见 `docs/视听升级计划0911.md`。

1. `src/core` 禁止 import `phaser` 或任何浏览器 API（`window`、`document`、`performance`）。逻辑层只吃 `InputFrame`、吐 `WorldState`。
2. 逻辑固定 60 Hz。所有帧数据、硬直、速度以"逻辑帧"为单位，不用毫秒。
3. 确定性：位置与速度用整数（单位 1/256 像素，常量 `SUBPIXEL = 256`）；随机数只用 `core/Rng.ts` 的可种子生成器；禁止 `Math.random`、`Date.now` 进入 core。
4. 角色 = 数据。新增角色只允许新增 `src/characters/<id>/` 与 `public/assets/characters/<id>/`，不改 core。
5. 判定框统一 `[x, y, w, h]`，原点角色脚下中心，面朝右，y 向上为负；镜像由 core 处理。
8. 输入层必须锁存按键：一次按下-松开短于一逻辑帧时，下一次 `snapshot()` 仍要返回该键一帧。
6. 不为让代码跑起来而注释报错或加 `// @ts-ignore`；找根因。
7. 版权素材（官方精灵、原声）只放本地 `public/assets/`，已在 `.gitignore` 中排除；占位素材和自制素材可以提交。

## 命名

- 文件：类 / 场景 `PascalCase.ts`，其余 `camelCase.ts`，测试 `*.test.ts`
- 招式 id：`snake_case` 英文（如 `gomu_bazooka`），显示名放 `name` 字段
- 状态 id：`snake_case`（`idle`、`walk_fwd`、`jump_neutral`、`hit_stand_high`）
- 动画 key：`<characterId>/<stateId>`

## 验证命令

改完必须跑，全绿再算完成：

```
npm run typecheck   # tsc --noEmit
npm run lint        # eslint
npm test            # vitest run
npm run build       # vite build
```

开发预览：`npm run dev`，默认 http://localhost:5173

本地游玩入口：根目录 `开始游戏.cmd` 调用 `scripts/playCandidate.mjs --release candidate-0913 --full`，只在本机启动已验证的 `.local-releases/candidate-0913/` 并打开完整动漫标题；`开始旧版游戏.cmd` 调用 `scripts/playLocal.mjs` 服务保留的旧 `dist/`。不安装依赖、不自动构建或发布。维护入口时保持这一边界。
入口复用已占用端口时必须核对完整构建内容标识及首页哈希，不得只凭相同游戏标题判为当前版本。运行中磁盘构建改变时明确要求重启，不能混用新旧资源。该机制可先修复，不代表已获完整动漫游戏放行或切换默认画风。

内部样板入口：根目录 `体验新版样板.cmd` 保持调用 `scripts/playCandidate.mjs --release candidate-0913`，只服务 `.local-releases/candidate-0913/` 并打开 `?art=anime&mode=training`。完整游戏按钮显式增加 `--full`，打开 `?art=anime&scope=full&quality=high` 标题；日志区分完整候选和样板。启动器不构建、不下载依赖、不发布。0912旧样板仍可通过 `node scripts/playCandidate.mjs --release candidate-0912` 打开；`--full`不支持0912。候选不存在或占用端口不是相同完整内容与首页哈希时明确报错。

## 调试快捷键（游戏内）

F1 判定框 · F2 帧数据面板 · F3 输入显示 · F4 精灵 / 色块切换 · M 静音
训练模式（`?mode=training`）：F5 木桩行为循环（含受身 tech） · F6 位置重置 · F7 无限气 · F8 无限血 · F9 慢放 · F10 逐帧 · F11 出招表 · F12 跳过引导

## 里程碑与范围

当前阶段和验收标准见 `docs/PLAN.md` 第 6 节。第一版锁定 2 角色、1 舞台、1v1。当前执行 `docs/游戏改造计划0913.md`，自主完成内部检查并继续，用户集中试玩不再成为中间扩产前提；旧美术约束中固定像素路线的部分已被用户要求转向取代。`docs/游戏复审与改进方案0911.md` 和 `docs/项目优化计划0911.md` 的输入、攻防、连续动作及流程修补保留。超出范围的想法记到 `docs/PLAN.md` 的 M7 清单，不直接开工。

## 清理

- `dist/`、`node_modules/`、`.vite/` 不提交
- 临时脚本、试验文件放系统临时目录，不放仓库
- 一个里程碑结束时更新 `docs/PLAN.md` 的文档状态行和里程碑表
