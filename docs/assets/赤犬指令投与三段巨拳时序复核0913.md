# 赤犬指令投与三段巨拳时序复核0913

这份记录为尚待新人物原图接入的冥狗、冥狗·终焉和大喷火·连准备真实接触基线。它不表示三招美术已完成，也不代替浏览器画面、声音与完整比赛验收。

## 输入与复核范围

系统临时脚本 `opf-akainu-command-super-full-trace0913.ts` 使用当前 `FightSim` 和正式两角色数据，运行三招 × 两朝向 × 两个场地位置安排 × 五种对手情境，共60例。情境包括近身、接近边距、远处挥空、近身后防和近身小跳；两个位置安排为普通投挣气后所处位置，以及双方以真实行走回到中场。

终焉的300气通过七次真实普通投取得；大喷火·连的气通过三次真实普通投取得。没有直接写入状态、坐标、血、气或命中结果，没有改任何伤害、攻击范围、招式时长。回中场后留24逻辑帧中立输入，避免两段后退被正常输入系统识别为后撤步；这是测试输入的修正，不是绕过游戏输入。

结果写入 `%TEMP%/akainu-command-super-full-review0913/actual-core-command-super-full-traces.json` 和 `trace-summary.json`。前者保存逐帧状态、坐标、速度、真实接触事件和路飞实际动画索引，后者保留摘要。60例攻击方最后均回到idle；对应左右朝向的接触种类、伤害、发生时刻与恢复时刻完全一致。

## 指令投：抓住以后必须切换独立时钟

冥狗按真实623A输入，第8逻辑帧抓住后进入throw，`stateFrame`回到0；第26个throw帧才放手并结算140伤害，因此本次输入追踪是总tick34产生hit、tick48回idle。落空时不进入throw，原攻击38帧后结束。后防不能防住该指令投，小跳能令这一测试安排下的抓取落空。

终焉按真实236236B输入，第12帧抓住后进入throw；throw第70帧放手并结算480，因此总tick82产生hit、tick102回idle。落空走原攻击48帧。后防仍可被抓，小跳安排能脱离抓取。

正式渲染的 `currentAnimation` 已区分 `exposures` 与 `throwExposures`。新图清单不能把抓持、蓄力、爆裂放手直接填进原攻击时间轴，否则抓住瞬间会把起手重复播放，或在真实放手前画出爆裂。命中火花、爆炸和声音只在实际hit事件出现；throw事件本身不产生伤害。

已重新比对用于生成导图的路飞受抓姿势与当前 `public/assets/characters/luffy/anime/runtime.json`，其根点和胸口连接点一致。因此冥狗抓持掌心目标仍为相对赤犬脚根(+27.6894,-58.7677)，终焉为(+29.6894,-58.7677)。不能用对手脚根间距34/36代替实际胸口位置。

## 三段巨拳：三次出拳不等于每次都命中

大喷火·连保留原84个动作逻辑帧；真正有效段起点为14、29、44。当前输入基线中，命中定格会延长现实经过的逻辑步数，三次都接触时的记录tick为14、43、72。

- 近身墙边站立目标：三段均中，伤害105、94、84；tick126回idle。
- 近身中场站立目标：前两段中，第三段因目标已被实际击退而落空；tick112回idle。
- 接近最远边距的站立目标：仅第一段中；tick98回idle。
- 远处：三段均挥空，没有接触事件；tick84回idle。
- 近身后防：三段均产生真实block，伤害均0；tick126回idle。
- 本次近身小跳安排：三段均接触空中目标，伤害105、94、84；tick126回idle。

因此新人物需要三个实际拳形，但不能固定播放三次击中爆炸、三次命中声或三次受击反应。三连拳在中场对站立目标只中两段是本次查到的现有战斗行为；先保留作为完整对局中的可玩性复核项，不为原画长度改判定或数值。

## 尚待完成

三组新原图仍依赖已核实的官方GPT Image 2.5网页生成。配对技术导图和提示索引为 `%TEMP%/akainu-production-prompts0913/command-super-paired-guides0913.json`。图到后仍须完成同手连接、真实掌点／拳形范围、绿底清理、源像素保护、双人接触、收招与正常速度检查，再交中央清单集成。当前没有用旧像素图、重复静帧或程序新画身体填补这三组能力。
