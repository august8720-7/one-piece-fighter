# 大喷火候选加工记录0912

状态：**已写入候选清单，未据此标记美术或真实操作验收通过。** 本次仅更新赤犬子树；由主任务统一打包。详细源图、网页型号依据、完整提示词、会话、SHA256、裁切坐标和保护区哈希见同目录 `大喷火加工来源凭据0912.json`。

## 采用内容与边界

采用返工四格的中立、蓄力、最长打击、回收，另复用首稿第3格发力与第6格归位。首稿第2/5格换能力手，不映射；两份原始PNG完整保留。所有输入来源均为已记录官方网页型号依据的 GPT Image 2.5 原图。

返工图用中立参考裁框 `[120,0,460,445]`，初始边缘抠除测得alpha框 `[22,15,439,427]`、高412源像素，整张统一 `unitScale=924/412`，角色比例仍为0.22。后续去绿只清理背景与色溢，不据此重新缩放身体。首稿统一中立高度为332源像素，整张 `unitScale=924/332`。

返工图根点人工登记为左/右列x=348/1202，第一/第二排地面y=426/892。首稿列x=318/1042，地面行y=346/702/1054。根点没有为了凑射程移动；它们是人工配准值，不能当作模型给出的骨骼信息。

## 唯一局部变换

- 有效画裁框：`[80,466,840,442]`；裁内根点：`[268,426]`。
- 仅横向延展裁内x420..640的既有熔岩中段，由220变419源像素，即1.904545倍；裁内x640之后的整块拳部平移199源像素，大小和像素不变。
- 变换区：`[420,0,420,238]`。保护区：`[0,0,420,442]`及`[420,238,420,204]`，覆盖普通身体、肩袖、非能力手和脚。画布只由840×442增加透明空间至1039×442。
- 人工拳尖点`[799,114]`的屏幕相对位置由`(+261.994,-153.940)`变为`(+360.181,-153.940)`。真实判定仍为x`+40..+360`、y`-220..-80`屏幕像素，未改招式或引擎数值。
- 原图/变换图保护区逐像素相同、拳部逐像素相同，肩到拳的透明通道连通检查通过。根点、肩和拳的高度不变。熔岩岩壳被横向拉长，实际尺寸下可见；这是已披露的候选限制。

背景清理使用显式绿孔种子、容差64和3源像素边缘去绿。有效画另有一处经放大目检的5×2源像素封闭绿底孔，裁内`[595,156,5,2]`在变换前排除，未触及岩壳、火线或身体。六画在输出尺寸均没有通过以下阈值的残绿像素：alpha≥128、G>120、G>1.5R、G>1.5B、R/B<100。该数值检查不能替代画面目检。

## 原57帧时序与取舍

| 顺序 | 画面 | 逻辑帧数 |
| --- | --- | --- |
| 0 | 中立起始 | 2 |
| 1 | 同手蓄力 | 12 |
| 2 | 发力前沿 | 6 |
| 3 | 最远实体熔岩拳 | 5 |
| 4 | 熔岩臂回收 | 28 |
| 5 | 普通护架归位 | 4 |

仍为20起手、5有效、32收招，共57逻辑帧。把收招的16/16改为28/4，是为了避免普通护架已恢复却仍有约0.267秒不能行动的误导。最后4帧是归位过渡；没有减少收招或延长有效攻击。实际回收观感仍待浏览器正常速度验证。

TEMP候选素材目录：`C:/Users/august/AppData/Local/Temp/magma-calibration-0912/`，包含六画接触表、原/处理图和`大喷火57帧正常速度候选0912.gif`。GIF受厘秒精度限制，六段为30/200/100/90/460/70毫秒、共950毫秒；运行时清单使用精确逻辑帧，不使用GIF播放时长。

## 已完成检查

独立`magma-straight`变换测试5项，原`rubber-straight`测试9项、图集测试4项通过。Rubber仍禁止伸长，不复用或放宽其限制。类型检查与改动文件lint通过。赤犬源凭据校验及`--check`内存组装通过：19张独立裁图、23个映射帧、10个动作，预计图集1024×2048。

这些检查只证明输入来源、机械约束和清单可组装。实际236输入、命中/格挡/打断、墙弹、按键受身、重置与持续游玩的画面/声音验收由主任务统一构建后进行。
