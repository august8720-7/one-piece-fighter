# 赤犬空中双拳接触复核0913

当前状态：原稿两个活动姿伸过范围；返工轻拳虽缩回真实通道，经实际格挡特效与独立代理复核仍像架手，需只修一个有效姿。返工重拳收过短，保留候选并准备单姿向前肘路线。尚未将 `j_a/j_c` 声称正式完成。准备和回收保留，新增原画必须继续经核实的 GPT Image 2.5 网页入口。以下核心输入检查不自动证明原图美术合格。

## 来源与独立标尺

- `public/assets/generated/source/akainu-air-punches-0913.png`，实际1024×1536。
- SHA256 `a04a0eb02da34fde76b263b768629c2fac9b784526e2e3efc03507ab9049ca60`。
- 会话 <https://chatgpt.com/c/6aa69565-b43c-83ea-87ee-d4e46a466709>。
- 凭据/完整提示：本目录 `赤犬空中双拳型号凭据0913.txt`、`赤犬空中双拳原始提示词0913.txt`。
- 中立实际去绿高度444原图像素，统一 `unitScale=924/444`，不按每格外包围框分别缩放。
- 同源中立骨盆 `(274,242)`、支撑根点 `(264,457)` 建立偏移 `(-10,+215)`。后续空中姿使用同一骨盆到虚拟根点偏移；抬起后的鞋底不能替代虚拟站姿根点。
- 五个动作分别为准备、普通空拳、普通回收、熔岩空拳、熔岩回收；两链都来自粉花侧的同一肩，另一手守在胸前。

## 范围诊断

| 活动姿 | 不变的实际判定 | 实测掌心 | 实测腕点 | 实测拳前点 |
|---|---|---|---|---|
| j_a | `[10,-80,32,18]` | `(55.8562,-75.7722)` | `(50.3622,-76.2300)` | `(60.8924,-75.7722)` |
| j_c | `[10,-84,46,28]` | `(59.2900,-79.6638)` | `(52.8803,-79.8927)` | `(65.6997,-79.6638)` |

空轻拳的腕已超过判定最前x42，整只实体手套都在判定外，这会导致看似打到却没有真实命中的情况；重拳掌心和拳前点也超过最前x56。不能通过移动根点、整体缩小角色、缩小拳、拉伸肢体或改判定掩盖问题。

针对当前原图的返工引导：`%TEMP%/akainu-production-prompts0913/akainu-air-punch-elbow-repair-guide.png`，SHA256 `e650c1c1fd101ce26e9d44ea4284396f9f3c2c8e52f76186448091d90647b223`。配套 `赤犬空中双拳收肘返工-未提交提示词0913.txt`。只改变第3/5格同一手的自然肘角；轻掌从原图 `(519,592)` 到 `(426,602)`，重掌从 `(520,1047)` 到 `(463,1077)`，保留肩、身体、骨盆、双腿和其它四格。引导的线/框仅是技术说明，不是运行时原画。

## 透明处理与测试准备

所有六帧强绿残留为0。已看过肩章小缝、衣腿间隙及岩浆小孔的放大图，再逐项写入显式处理区。相邻格的披风、拳和火星以源坐标的局部排除矩形去除；本角色与当前活动手臂在这些矩形外，不用全图抹色处理。

真实输入脚本已经跑通36例：P1/P2 × 空轻/重拳 × 近/边/远 × 空对空/下降打站立目标/下降打站防目标。只通过行走、短按上、攻击键和定时后防产生战斗；没有写入状态、位置、血、气。近/边按当前数据命中或格挡，远处没有事件，全部落地后恢复idle。站防输入允许木桩自然后退，所以格挡当刻实距与起手前不同，记录里保留实际值。

空轻拳：空对空 tick11、下降 tick20 接触，伤害38；重拳：空对空 tick15、下降 tick21 接触，伤害80；格挡伤害0。这些是测试输入安排下的真实事件时刻，不是修改后的技能时长或浏览器输入延迟。新素材完成后将复用这些相同输入看配对接触、升降根点与落地收招。

工作目录 `%TEMP%/akainu-air-punch-review0913/` 包含原图坐标图、透明裁切、`inspection-report.json`、`range-metrics.json`、`source-protection-proof.json`、`input-probe-traces.json`。`reference-fragment.json` 的运行时动作和帧为空，只保留来源参考；`candidate-fragment.json` 含未采用的诊断姿势，不得直接合入中央清单。未称完成两招或整款游戏。

## 收肘返工后的实际检查

原图 `public/assets/generated/source/akainu-air-punches-elbow-repair-0913.png`，SHA256 `8c61f07221fefe5f39e69c17f015bd6dcb63cd2d8aec11863b473b58b5018c79`，会话 https://chatgpt.com/c/6aa6a18e-8138-83ea-9e75-fbc402b37b1a ，凭据/提示前缀 `赤犬空中双拳收肘返工`。新图独立中立444px，仍是924/444；虚拟根保持中立骨盆偏移(-10,+215)。

返工 j_a 拳心(+32.7354,-75.7722)，拳尖(+35.9403,-79.6638)；返工 j_c 拳心(+33.4222,-73.9408)，拳尖(+38.4584,-77.1457)。六个裁切强绿残留0，582449个非绿原画像素逐字节一致。显式排除矩形只去掉邻格侵入的披风/拳火碎片，自己人体完整；未对原图肘、手或身体作几何变形。

36例真实走近、短按Up小跳、A/C测试：两朝向×两招×近/边/远×空对空/落地命中/落地站防。实距近35.5664，j_a边52.5586、j_c边67.8516，站防延迟开始时自然多走1.8008；远距90目标实走至约89.94。j_a空对空第11帧、下落地面第20帧首次接触，命中38；j_c分别第15/21帧，命中80；站防均0，远距均无命中。所有例最终落地idle。没有修改坐标、状态、血气或原帧数据。

轻拳空对空事件前进入对手通道，事件后受击后仰造成分离，不能据此单独要求返工。地面站防时拳略在帽上，根与另一代理同意补看游戏现有blockSpark/SkillEffects.contact再判断，不能仅靠无特效逐帧图要求拳覆盖整个hurtbox。该检查未完成前不正式接入。

重拳返工在空对空边距与落地站防边距都明显收在胸侧，属于主形过短；目标改用(+46,-68)，肘向前而不向上、拳不高于嘴部的单姿补画。真实双人站防图与骨骼意图已制作在 `%TEMP%/akainu-production-prompts0913/akainu-air-heavy-paired-elbow-guide.png`，提示 `赤犬空重拳配对肘向前单姿-未提交提示词0913.txt`，未提交前不得计入生成完成。

TEMP `akainu-air-punch-repair-review0913` 保留实际runtime/atlas、`candidate-fragment.json`（两招均诊断）、`actual-core-air-punch-traces.json`、各上下文接触前/事件/后图、远距挥空与轻拳正常速度GIF、保护证明。所有媒体为实际核心数据离线合成，不是浏览器实机或帧率证据。

## 现有实际接触特效补查

已将其中 j_a 的18组真实输入追踪交给当前生产 `SkillEffects.contact` 与 `Particles.hitSpark/blockSpark` 类处理，使用图像和图形替身只收集原类输出的绘制参数，再与实际人物根点合成。没有重新设计、放大或移动特效，也没有改拳头、根点与战斗数据。画面同时包含真实防御环、命中图片以及实际粒子线条／闪光；此前仅看人物裸图的缺项已补上。

产物在 `%TEMP%/akainu-ja-std-runtime-fx-review0913/`：`actual-core-and-runtime-fx-traces.json` 共30例（18例 j_a、12例 st_d），`j_a-*-first-contact-or-far-miss-runtime-fx.png`、`j_a-*-edge-before-contact-after-runtime-fx.png` 与正常速度双朝向GIF。脚本为系统临时目录的 `opf-akainu-ja-std-runtime-fx0913.ts` 与 `opf-akainu-ja-std-runtime-fx-render0913.py`。

内部检查看到：空对空首接触的手部、冲击和受击躯干可连起来；下降站防时拳仍比帽缘略高，但现有局部防御环连接了拳与头部／前臂。远距离没有凭空接触效果。先交总集成与另一代理复核，暂不据此自动宣称正式覆盖；j_c 过短的问题仍保留。这里是调用真实表现类的离线绘制输出，仍不是 Phaser 浏览器实机或主观听感验收。

独立代理随后指出：轻拳有效姿的前臂依然竖起，拳在帽／格挡手上方，读起来更像向上架手。接受该反例，撤回直接保留的倾向，不增加现有火花来遮盖。下一次只修 `air_light_active`：同一手肘向前略下送，前臂末段近水平，闭合拳面朝右前下；目标拳心(+36,-66)、拳尖约(+40,-65)，其余身体和跳姿保持。目标仍由真实范围约束，不要求拳形追踪敌人。

导图、原有效姿参考与提示集中于 `%TEMP%/akainu-production-prompts0913/ja-std-actual-fx-single-repair0913.json`。轻拳提示 `赤犬空轻拳实际格挡定向单姿-未提交提示词0913.txt`，附件 `akainu-j_a-active-original-reference.png`、`akainu-j_a-real-fx-contact-repair-guide.png`。本段是待生成的具体修正，不是完成记录。
