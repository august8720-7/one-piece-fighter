# 赤犬普通投分层接入复核0913

状态：普通前投、背投的抓持接触及原像素分层通过内部检查，根任务已将片段合并至中央清单、重建 0913 候选。仍须验证新构建的真实浏览器动作全过程及完整对局；当前候选仍是内部训练样板。未增加或修改战斗数值。本说明不把其他尚未制作招式计为完成。

## 原图与尺度

新抓持使用 GPT Image 2.5 官方网页生成的 `public/assets/generated/source/akainu-throw-contact-guide-0913.png`，SHA256 `d397d9b298ebc2510ec646c84c3162cfb2ae612bf24857ca22f20851768a10ad`。会话：https://chatgpt.com/c/6aa68126-0ba8-83ea-b676-7bb871b2b97d 。原始提示和生成前型号凭据是 `赤犬实际配对抓持原始提示词0913.txt`、`赤犬实际配对抓持型号凭据0913.txt`。本次真正上传实际游戏配对引导图，文字思考为非Pro中档，图像型号仍按官方 Image2.5 入口核实。

新图只有一个姿势，不能谎称有中立标尺。裁切[205,490,632,610]，实际人物透明高度586px。整张源尺度通过上传引导图里同一低位抓持姿的身体头顶→支撑脚标定：旧抓持437px、旧源unit924/445；新源unit=(924/445)×437/586=1.548444989837788。后支撑鞋相对根点偏移由同一参考145px等比换算成194.43935927px，新根点绝对[483.43935927,1086]。标定只用身体与支撑脚，未用掌胸距离拟合；没有肢体变形、移动根点追手或修改双方距离。

新抓持透明处理强绿残留0，184924个非绿原像素保持逐字节一致。

释放复用 `akainu-throw-low-repair-0913.png` 的真实张掌姿，source SHA `a2dc9b28ff57d501e12c32b4befab45e108f7658e76c4dbca67d21c20697f2af`、unit924/445、原crop[216,974,602,485]及根点[452,1443]不变；只在真实释放后使用，不能再次用来假装近距抓住。回收复用 `akainu-throw-low-victim-0913.png` 的真实回收姿，source SHA `1ec85a7378f664a87aa6bdf8a98a46003be833cc2bcab3a37193e2a6510cf737`，unit924/443；不是重命名idle。

## 接触与分层

真实赤犬抓持间距38。新主掌相对路飞实际thrown胸点dx+0.93895321、dy-0.16606857，距离0.95352604世界像素。副掌在胸点前17.80151915、上3.74297649，画面落在远侧红袖/前臂附近，作为环抱另一侧的手可读。

整张赤犬置前会完全遮住路飞脸，因此不能直接采用。最小修复是把抓持的近侧前臂和手套按显式多边形分成前层：赤犬身体在后、路飞在中、近侧前臂在前。根任务、赤犬代理和路飞代理均独立查看左右对照，确认脸、红衣胸、挣扎双臂与抓手都可读。

背投释放后也发现整张身体遮住路飞首个hit_air反应，因此释放的原前臂/张掌同样分层。前投和背投在24tick的脸、身体与抛出方向清晰，29→30tick回收时角色已分离；未改朝向、位置或投掷逻辑。普通后投依照真实核心在24tick换边，受击者短暂横过施投者前方，保持这个真实运动。

两帧分层均不绘制新像素：先把整帧一次性清理/缩放，再将源裁切坐标的二值多边形mask用NEAREST变换到运行时大小，按mask分配完整RGBA像素。前后层同画布、同根点；源层与运行层都验证重叠0、丢失0，重合成与原帧的哈希相同。抓持前层源9535/runtime4481像素，释放前层源5784/runtime5090像素。具体点列、哈希与原图绑定见下述本地归档的两个foreground-layer-candidate JSON；正式authoring字段为foregroundPolygon和foregroundNote。前层是已有动作的辅助图片，不增加动作覆盖数。

独立打包器复核的抓持完整 RGBA 与重组 SHA256 均为 `562c0be0f8b962274ad26a173cc1d4ff3ccf371954e20a2a0884f41287e66ef0`，释放均为 `a9fb05d2294f2f6f8fb42c8584b5fffc6052163ec985d3e911f3195c5fc75c33`。这些是处理后的完整帧像素哈希，与上文原始下载文件哈希用途不同。

## 动作与真实核心验证

普通投总48逻辑帧：抓持24、释放6、回收18。释放伤害仍在24tick；因为普通投state开始时已抓住对手，不加入错误的高位探手阶段。拆投复用现有真实throw_tech动作，成功拆投后立即退出分层抓持。

新打包器已经将最终候选构建至TEMP的accepted-layered，运行时严格校验通过。16个真实输入组合再次通过：双方角色主投×P1/P2×前后投×拆/不拆。仅通过步行靠近后方向+C，没有直接写HP、位置或状态。持有距离路飞34/赤犬38，双方持有阶段接地；释放帧路飞22/赤犬24，伤害110/120；拆投无伤害，双方最后恢复行动。运行时动画与前景映射都来自最终实际打包结果。

最终普通投新增3个映射名throw/throw_fwd/throw_back，实际原图姿势3张、可选前景姿势2张；普通投本身含三个真实姿势，没有以复制静帧充数。当前合并基线候选单页4096×4096、RGBA内存67108864字节（64MiB），需在整体性能验收中继续核实；不据此声称60FPS已验收。

## 工程集成与归档

`manifest-fragment.json` 带三个来源、三个frame、两条foregroundPolygon、三个anim；根任务已完成中央清单合并和候选重建。集成代理实现了严格可选的 `foregroundFrames`：辅助图必须与原动作同尺寸、同根点、具备合法分页，缺失时报错并隐藏相关显示，不静默退回遮脸画面。真实普通投状态中，声明了分层的抓持和释放帧使用身体后层、手臂前层；进入普通回收、拆投、受击或隐藏时清除前层。前层跟随原图位置、朝向、密度、透明度及染色，仍由 Phaser 场景管理生命周期。

相关 55 项测试分批通过，覆盖真实输入普通投/背投、换边释放、拆投、资源损坏恢复、普通受击、共享变换、核心快照不变及不增加假动作覆盖。根任务在合并后报告 `typecheck`、`lint`、`test`、`build` 全通过，其中测试为 44 个文件、465 项；该结果是根任务工程验收记录，不替代本节未完成的浏览器验证。

2026-09-13 已按复制前白名单，将采用证据归档至 `.local-releases/改造验收-0913/赤犬抓投分层0913/`：66 个文件、67,269,579 字节，逐文件来源/目标 SHA256 和长度均一致，`allVerified=true`，原 TEMP 文件保留。目录先写 README 结构约定，再复制清单内容；不删除或覆盖不同文件。

- `archive-plan0913.json` 保存复制前来源、长度和 SHA256；计划文件 SHA256 为 `c8e80e6c4b848c58f8551b4e6e19bf7695e20a4e9cd4649a7fbc2774745157ba`。
- `archive-verification0913.json` 保存复制后逐文件校验与原文件保留结果；文件 SHA256 为 `30276216acbcc3431015d0302af73d981c0992808a8b7a2e1b39b25c9ca42480`。
- `accepted-layered/` 是最终离线赤犬构建，`luffy/` 是配对参考构建，`foreground-builder-verification/` 是独立像素二分复核。
- `actual-core-layered-throw-traces.json` 已再次解析，含 16 个结果和 16 条轨迹，明确只用真实靠近和方向+C输入。
- 两张 `actual-core-*-layer-comparison.png` 保存分层前后对照；`final-release-P*-*-tick*.png` 为 28 张采用结果释放/回收节点；`final-layered-*-normal-speed.gif` 是按真实 60Hz 时序输出的离线正常速度前投、背投和拆投。

旧失败候选、原图、旧无分层诊断文件全部保留；归档未将早期 `actual-layered-*` 媒体标作采用结果。

## 实机证据边界

本次归档证明素材、离线配对、原像素完整性及纯逻辑输入验证；尚不证明浏览器技能全过程、音轨、完整比赛或持续60帧体验。根任务已报告 4177 候选主脚本为 `index-As3dlZdW.js`，但独立 Edge 在刷新至该构建前被“页面上有其他扩展 UI”阻断，未完成新分层构建的实机检查。旧内置浏览器策略错误曾自然恢复，不能据此推断独立 Edge 当前也已恢复。

阻断前独立 Edge 的页面报告可见且有焦点，但四秒 RAF 采样仅四次回调，约每秒一次；当时尚未开始录制，所以不能归因于 MediaRecorder。实际原生窗口前台状态和原因仍未知，不称为游戏性能结论。临时音轨捕获挂接已解除，未更改用户音量。浏览器音画及完整比赛由根任务统一验收，不能用此处离线正常速度 GIF 替代。

归档完成后的单次正常刷新重试仍收到同一扩展 UI 阻断；没有绕过控制，没有新录制，也没有将新构建标为已实机验证。
