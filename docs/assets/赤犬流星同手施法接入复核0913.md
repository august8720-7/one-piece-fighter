# 赤犬流星同手施法接入复核0913

结论：流星火山施法人物可接入当前候选。无花侧的同一条手臂完成蓄势、向上发射和回收，另一条普通黑手套手保持完整；普通3发和强化8发全部发生在发射姿期间。此检查仅放行施法人物原图与核心同步，不代表完整流星技能、声音、浏览器性能或整款游戏已全部验收。

## 采用素材

1. 准备/回收采用 `public/assets/generated/source/akainu-meteor-same-arm-repair-0913.png` 的第2/4格，SHA256 `f05b3b3a6c1d29390a92b5f01996eca650dde6d00b958c6a5b505e954eb963c6`。会话 <https://chatgpt.com/c/6aa686bb-3520-83e9-bced-2fea49f3f310>。凭据/提示：`赤犬流星同手返工型号凭据0913.txt`、`赤犬流星同手返工原始提示词0913.txt`。
2. 发射采用 `public/assets/generated/source/akainu-meteor-single-guided-0913.png`，实际1024×1536，SHA256 `8eb18523ebc2513ab1327d67a0b86d33c5cbbb4b30ee4853cac5b993b578a557`。会话 <https://chatgpt.com/c/6aa69c7c-0950-83e9-ad84-336c187f2d77>。凭据/提示：`赤犬流星单姿骨骼补画型号凭据0913.txt`、`赤犬流星单姿骨骼补画原始提示词0913.txt`。

全部新增AI素材来自核实的 GPT Image 2.5 官方网页。单姿补画使用技术引导 `akainu-meteor-single-pose-arm-guide.png`；它是由原图裁切及两肩连接说明组成的制作参考，不作为运行时原画。

首稿及同手返工的第3格仍不采用，详见 `赤犬流星施法复核0913.md` 与 `赤犬流星同手返工复核0913.md`。首稿第2格的邻格火星部分叠在鞋边，不能删除火星同时假装保留鞋像素；因此本次改用返工图已经干净的第2格，重新做独立裁切、透明与像素保护检查，而非套用首稿通过记录。

## 比例、根点与透明化

返工图标准中立去绿后实测394像素高，对主设定924，整个来源 `unitScale=924/394`。准备/回收保留同一源的后支撑脚偏移135原图像素，准备绝对根点 `(434,817)`，回收 `(433,1735)`。

单姿发射没有中立姿，因此只用身体的帽顶到后支撑鞋底校准，不把举起的熔岩拳和火焰计为人物身高。旧站立施法身体从y907到1307为400原图像素；新单姿从y498到1344为846像素。新来源 `unitScale=(924/394)*(400/846)=1.1088310472693235`，对应身体高度103.19世界像素左右。此处只是复用旧姿的身体标尺，旧姿错误手臂未接入。

新后支撑脚 `(207,1344)`，沿同一身体比例换算支撑偏移 `135*846/400=285.525`，根点 `(492.525,1344)`。没有因拳的位置移动根点，没有单独放大拳、身体或改手臂。原始裁切 `[89,173,872,1182]` 完整保留举拳与双脚。

三幅正式姿及返工中立参考强绿色残留全部为0；已检查熔岩缝、肩章碎缝、披风腿间的局部放大图，逐项声明除绿区。四幅合计699868个受保护非绿源像素在处理前后完全相等，原/处理像素序列的SHA256一致。正式姿没有程序绘制的新增身体部位。

## 实际核心同步

| 招式 | 曝光：准备/发射/回收 | 真实投射物发射逻辑帧 | 总长 |
|---|---|---|---|
| sp_meteor | 18/9/17 | 18、22、26，共3发 | 44 |
| sp_meteor_rain | 20/29/11 | 20、24、28、32、36、40、44、48，共8发 | 60 |

两朝向 × 两招共4例。强化气量通过3次真实普通投各伤害120取得135气，再用方向输入走回舞台原侧；不写HP、气、位置或状态。全部投射物ID按真实事件计数，发射当时的实际运行时动画均为对应招式第1帧（发射姿）。最后一发之后普通第27帧、强化第49帧才换回收姿，最后均恢复idle。

执行了当前实际图集生成及严格运行时清单校验，结果通过。两侧首发、末发、回收和回到idle静帧已目视检查，人物尺度与后支撑脚稳定，肩部连续。已生成按真实60Hz时序的正常速度GIF供最终集成复核；它只显示施法者时序与真实发射计数，不伪称已显示全部投射物效果或实际浏览器60FPS。

## 交接与证据

工作目录 `%TEMP%/akainu-meteor-guided-review0913/`：

- `manifest-fragment.json`：可采用的三真实姿势、两招同步表及完整来源引用；由root合入中央清单。
- `runtime.json`、图集与atlas JSON：本次真实builder产物，严格校验通过。
- `actual-core-meteor-traces.json`：4例原始输入结果、投射物ID/帧、事件和恢复过程。
- `sp_meteor-normal-speed-both-facing.gif`、`sp_meteor_rain-normal-speed-both-facing.gif`：正常速度双朝向。
- `sp_meteor-actual-first-last-emission-and-recovery.png`、`sp_meteor_rain-actual-first-last-emission-and-recovery.png`：首末发与回收对照。
- `source-protection-proof.json`、`all-green-residuals.json`、`inspection-report.json`及透明原尺寸裁切：原图处理证据。
- `charge-toe-neighbor-inspection.png` 与 `repair-charge-toe-inspection.png`：更换准备来源的实际理由。

TEMP复现脚本为 `opf-prepare-akainu-meteor-guided0913.py`、`opf-verify-akainu-meteor-guided0913.py`、`opf-akainu-meteor-guided-trace0913.ts`、`opf-akainu-meteor-guided-render0913.py`。中央配置、战斗核心和正式入口未由本子任务修改。
