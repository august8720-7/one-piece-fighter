# 赤犬蹲拳上勾接触复核0913

当前结论：蹲轻拳 `cr_a` 的原图裁切、同手动作、实际命中和格挡画面支持接入。`cr_c` 的源图及核心运行已检查，仍需确认上勾起段到高拳终态的接触过程；暂不把它计作完成。工程整体验收、浏览器运行与整款游戏仍由最终集成另行验证。

## 原图与标尺

- 来源：`public/assets/generated/source/akainu-crouch-punch-upper-0913.png`，实际 1024×1536。
- SHA256：`d17529b2287a80934b78197fb1d4fbfed715cfc149515e7a23800d2ea0176b1e`。
- 会话：<https://chatgpt.com/c/6aa6841d-faf4-83ea-9333-45968d46a64e>。
- 型号凭据与完整提示分别为本目录 `赤犬蹲拳上勾型号凭据0913.txt`、`赤犬蹲拳上勾原始提示词0913.txt`。模型记录为经网页标识核对的 GPT Image 2.5。
- 标准站姿去绿后实测高 444 原图像素，对应主设定 924，因此整张源图 `unitScale=924/444`。这不是提示词要求的尺寸，也不为每张动作单独缩放。
- 中立根点 x264、后脚底 x128 建立固定 136 原图像素支撑偏移；活动、准备和回收沿用此偏移，绝不移动根点追拳或判定。
- 原图明确是粉花同侧的手依次准备、打出、回收。未改形体、拳头大小、战斗伤害、范围或时长。

## 处理与蹲轻拳结果

采用帧 `crouch_punch_prepare / crouch_punch_active / crouch_punch_recover`，曝光为 5/3/8 逻辑帧，完整 16 帧，含真实命中定格后按核心状态自然延长。

六个裁切的强绿色残留均为零。原图内 566956 个受保护非绿像素在原尺寸处理前后完全相等；只做显式绿底/边缘透明化与统一尺寸重采样。证据见工作目录的 `source-protection-proof.json`，不能把该数目解释为图集有效像素或新增动作数量。

`cr_a` 真实判定为 `[12,-56,34,16]`，掌心世界坐标约 `(41.2054,-54.7116)`，最前测量点 x46.0127。拳体外轮廓有少量越出矩形上沿，这是原图轮廓和矩形碰撞的差异；没有设“轮廓必须完全填满矩形”的假门槛。判定判断基于实战配对里拳到胸/防御手的接触及挥空辨识，未靠数学包围框比例批准。

对 `cr_a/cr_c` 分别运行：赤犬 P1/P2 × 真实行走目标距 36/58/90 × 命中/定时蹲防，共 24 例。行走实际停在 35.56640625、57.65625、89.94140625 世界像素；不写人物位置、HP、气、状态。木桩在已知活动帧前一帧输入后下防，避免起手期间自行后退，这是测试输入而非 CPU 读键。

蹲轻拳 12 例全部正确识别招式；近距、边缘在 tick5 发生真实 hit/block，伤害35或0；远距无事件。命中/格挡后 tick23 回蹲，挥空 tick16 回蹲；松开下后回站立，三个真实视觉姿势依次出现。两侧图像的拳与胸部、防守前臂接触清楚，远距没有假命中图形。左右与近/中/远、接触前后已看静帧；60Hz 时序 GIF 已生成，仍须集成场景实际正常速度检查。

## 上勾当前边界

`cr_c` 保持 9/5/18，共32帧；真实框为 `[8,-120,36,70]`。掌心 `(38.0005,-116.0619)`，拳末端 y约-122.0138；这是向上举起的终态。

核心 12 例均在 tick9 正确命中85/格挡0或远处挥空，分别 tick43/32 回蹲。击飞命中后路飞立即切入 `hit_air`，所以仅看接触后的轮廓空隙不能据此否掉上勾；当前正在对照事件前后和初段出拳路径。不将核心测试通过或掌心落在框内作为美术完成证明。

## 接入文件与复现

工作目录：`%TEMP%/akainu-crouch-punch-review0913/`。

- `manifest-fragment.json`：仅采用 `cr_a`；上勾帧保留在参考区，不自动覆盖中央配置。
- `candidate-fragment.json`：含两招的诊断候选，不能不经检查整份合入。
- `actual-core-crouch-punch-traces.json`：24例真实输入与全部逐帧结果。
- `actual-cr_a-hit-contact-both-facing.png`、`actual-cr_a-block-contact-both-facing.png`：两朝向三距离接触。
- `cr_a-normal-speed-both-facing.gif`、`cr_a-normal-speed-keyframe-strip.png`：正常60Hz时序媒体和关键姿势。GIF用20/10/20ms三帧周期表达60Hz；不是浏览器帧率证据。
- `actual-range-overlays.png`、`actual-range-metrics.json`：真实框和原图测量，辅助诊断。
- TEMP脚本：`opf-prepare-akainu-crouch-punch0913.py`、`opf-verify-akainu-crouch-punch0913.py`、`opf-akainu-crouch-punch-trace0913.ts`、`opf-akainu-crouch-punch-render0913.py`。

本轮只新增显式素材片段、来源处理与QA记录，未修改战斗核心及中央清单。最终集成须归档上述本地证据并完成当前版本四项工程检查。

## 蹲轻拳归档位置

2026-09-13 21:28，蹲轻拳采用证据已按显式13文件白名单复制到 `.local-releases/改造验收-0913/蹲拳连击巨拳0913/akainu-cr_a/`，源文件全部保留，逐文件长度及SHA256一致。上一级 `archive-plan0913.json` 和 `archive-verification0913.json` 同时记录本批蹲拳、路飞连击与巨拳的35文件复制和校验结果。

本组只有 `manifest-fragment.json` 中的 `cr_a` 是采用对象。归档的原始24例轨迹、范围图和保护证明仍含当时 `cr_c` 诊断，以保留完整上下文；未将 `candidate-fragment.json`、任何 `cr_c` 媒体或上勾初次回收候选作为采用文件复制。这里不改变上勾的当前制作状态，也不把离线GIF称为浏览器实机验收。

## 蹲重拳采用已有同手前伸起段

新单姿 `akainu-uppercut-early-contact-0913.png`（SHA256 `0c945daeb99a6a4abbe69300af555e01aba459873d85a1f52f92b0719362f5d2`，会话 https://chatgpt.com/c/6aa6a49d-8fec-83ea-886d-d87606b9f689 ，型号/完整提示前缀 `赤犬上勾初段接触补画`）仍把拳举高。按原回收帽顶1076→鞋底1455的379px，对应新帽顶518→鞋底1318的800px，掌心约(+38.3,-95.8)，没有达到初段约(+38,-68)目标。本图保留，未当作成功初段。上述是标定诊断，未接入。

不继续同类高拳提示返工，转而测试本张已验收的真实前伸拳 `crouch_punch_active` 作为下部接触路径，同一出拳手、同一原图尺度、同一后脚固定根关系。最终序列：原蓄力9 → 已有前伸拳2 → 原熔岩上举3 → 原弯肘回收18，共32逻辑帧；活动期仍5帧，只能按原核心命中一次，没有添加伤害。复用的是有明确前伸形体的真实出拳，不是重复idle或补假帧。

12个真实输入案例（两朝向×35.5664/57.6563/89.9414距离×命中/蹲防）保持第9帧首次接触、命中85、格挡0，远距无碰撞，正常回蹲后松开Down返回idle。命中定格自然延长可见接触，前伸姿停在防守者手臂/脸前，随后抬升；没有改状态、位置、血气或战斗数据。内部检查了接触前、事件、抬升和回收两朝向图，拳臂连接连续、后支撑根稳定。正常速度离线GIF保留，不当作浏览器完整验收。

可合入片段位于 `%TEMP%/akainu-crouch-punch-review0913/upper-forward-initial-candidate/manifest-fragment.json`，只映射cr_c。同目录保留 `actual-core-crouch-punch-traces.json`、严格runtime校验后的atlas/runtime、`cr_c-forward-contact-rise-recover-both-facing.png`、命中/格挡正常速度GIF及前后事件图。使用的四帧原像素已经被上文同一来源的保护区证明覆盖。此前 `upper-recovery-initial-candidate` 仍是短拳诊断，不能当采用版本。
