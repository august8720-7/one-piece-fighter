# 赤犬犬头发射与地裂人物复核0913

这次完成的是 `sp_inugami` 与 `sp_ground_split` 的人物素材内部接入检查。两张新原图中各有两个姿势换了攻击手，已明确排除；采用真实的同臂原画及已有同臂收招，未绘制补洞、拉伸人体或更改战斗数据。正式浏览器的声音、镜头、完整比赛验收仍由总集成继续完成。

## 原图与尺度

| 原图 | SHA256 | 网页会话 |
|---|---|---|
| `public/assets/generated/source/akainu-inugami-caster-0913.png` | `f65bfb3e6b781f79a491ede233b72ebadcee03af7e657009871b8f1316e9cff5` | https://chatgpt.com/c/6aa6b439-457c-83ea-9fbd-7e37663f101d |
| `public/assets/generated/source/akainu-ground-split-caster-0913.png` | `320cdb2f5e68c1d4fc4938bdab2b45feb9b5687c4d2bb3162ce1016a31d71dde` | https://chatgpt.com/c/6aa6b412-17d8-83ea-8ee7-186e590e8f5a |

网页型号凭据、完整提示词分别保存为 `赤犬犬噛红莲发射人物*0913.txt` 与 `赤犬熔岩地裂人物*0913.txt`，来源配置中记录凭据和提示词的 SHA256。两项均为已核实 GPT Image 2.5 网页来源。

犬头人物以原图中立高度 500 px 统一换算为基准 924 px，脚底后支撑点至根点的固定偏移为 156.5 px。地裂以中立高度 469 px 换算，固定偏移为 144 px。没有按拳头位置单独调人物比例或根点。地裂接地拳掌约在世界坐标 `(+37.3, -6.7)`，与实际向前接地后喷发的动作相符。

透明处理仅使用原图的显式绿底连通区域与边缘清理。两图各六姿势检查均为零绿色残留；犬头图的 728,903 个非绿像素、地裂图的 631,188 个非绿像素在处理前后哈希一致。这些计数包括保留但未采用的中立及错误手臂参考。

## 实际采用的动作链

| 招式 | 真实姿势与逻辑帧曝光 | 说明 |
|---|---|---|
| 犬噛红莲 | 低位同臂预备 14 → 前推 2 → 张掌回震 6 → 低位收拳 10 → 低守势 10 | 第 14 帧的前推对应真实发射；犬头是独立投射物，人物手臂不追踪敌人。 |
| 熔岩地裂 | 蓄手 8 → 下击 8 → 接地 6 → 同下击姿势回撤 8 → 低位收拳 8 → 低守势 8 | 第 16 帧接地对应真实活动段。回撤复用同一真实下击姿势，是反向收手，不是伪造新原画。 |

犬头原图的蓄手与站立收招、地裂原图的收招与防守均换臂，作为 `referenceFrames_NOT_BUILT` 保留，不进入动作序列。旧蹲拳原图的 `crouch_upper_recover`、`crouch_punch_recover` 提供同臂低位衔接；新增的 `inugami_low_guard` 同时用于两招末段。没有把中立图反复换名当作能力覆盖。

## 真实输入检查

运行 24 组测试：左右两侧 × 两招 × 三个距离 × 命中／蹲防。所有接近、出招和防守都通过实际输入完成，没有直接写入位置、状态、血量或气槽。

- 犬头距离实际为 35.56640625、98.4375、180 世界单位；`214+A` 每次仅发射一只犬头，均在 `stateFrame=14`、人物姿势索引 1。中距离命中在观察 tick 19、蹲防在 tick 18；远距离在 tick 37 接触。
- 贴身犬头在同一逻辑步发射并结束，所以已从真实 `projectileEnds` 补全追踪器记录。不能仅以该步末的投射物数组为空推断“没有发射”。这里只修了临时追踪器，没有改战斗核心。
- 地裂距离实际为 35.56640625、71.25、129.0234375；`22+B` 的前两个距离在 `stateFrame=16`、姿势索引 2 接触，远距离不命中。命中伤害 100，格挡伤害 0；没有产生投射物。
- 两侧均访问完整合法动作链，最终回到站立。严格运行时清单校验没有错误。总时长、伤害、范围和真实事件保持现状。

## 画面检查与证据边界

已检查双朝向的起手、发射／接地、回撤和归位图片，以及近中远实际接触合成。人物手臂连续，绿底无残留，犬头保留可辨认头部，地裂主体位于原有地面判定带。额外录取实际 `SkillEffects` 类的图像参数，使用只收集变换的图像替身输出后合成；并非手工扩大特效掩盖接触。

正常速度 GIF 按真实 60 Hz 追踪逐帧导出，采用 20/10/20 ms 的 GIF 时间序列表达每三逻辑帧 50 ms。媒体只是离线动作与特效变换复核，不能替代 Phaser 实际渲染、实际听感或浏览器正常速度整场验收。画面面板独立裁切，避免受击飞出画面后跨面板造成假象。

临时交付位于 `%TEMP%/akainu-dog-ground-ready0913/`：

- `manifest-fragment.json`：仅两项新动作的可合并片段，明确浏览器验收尚待执行。
- `actual-core-dog-ground-traces.json`：24 组真实输入、发射／结束／接触事件、人物根点与实际特效变换。
- `sp_inugami-*`、`sp_ground_split-*`：纯人物和含实际特效变换的双朝向阶段图、正常速度 GIF、三距离命中和格挡图。
- 透明与非绿像素保护证据分别位于 `%TEMP%/akainu-inugami-caster-review0913/`、`%TEMP%/akainu-ground-caster-review0913/` 的 `all-green-residuals.json` 与 `source-protection-proof.json`。

临时脚本为 `opf-prepare-dog-ground-originals0913.py`、`opf-clean-dog-ground0913.py`、`opf-assemble-dog-ground0913.py`、`opf-build-dog-ground0913.py`、`opf-akainu-dog-ground-trace0913.ts`、`opf-dog-ground-render0913.py`。后续永久归档由集成统一保留清单与 SHA256，不删除原始或失败候选。
