# 游戏公开部署0914

## 声音修复更新（2026-09-14）

- 声音修复源码已推送main（f93ba3c），运行包以追加提交34dc591f0ffaf38aa0b35b4546ddd22a111c73cc推送gh-pages，保留原历史和旧构建。
- GitHub Pages本次构建为built，错误为空。包目录为`D:/one-piece-fighter-pages-0914/2026-09-14T11-32-36-774Z/`，106份文件、47,410,609字节；104份网页/运行资源均通过公网HTTP200和SHA256，另2份为部署元数据。
- 新增音乐、待机、脚步和普通出力，页面提供音乐与音效署名链接。最终工程检查575项测试通过，独立Edge前台900秒检查通过。详细边界及媒体情况见[声音修复交付0914](声音修复交付0914.md)。

以下保留首次公开部署的历史记录。

用户于2026-09-14明确要求“可以公开，然后生成访问链接可以直接玩”。本轮采用现有 GitHub 仓库与 GitHub Pages，公开完整动漫运行包。

## 部署约定

- 仓库：`https://github.com/august8720-7/one-piece-fighter`。
- 目标网址：`https://august8720-7.github.io/one-piece-fighter/`。
- `main` 为源码，正式运行包单独放 `gh-pages`；不添加自定义 Actions 工作流。
- `scripts/packagePages.mjs` 从 `.local-releases/candidate-0913/` 提取首页、当前模块依赖、两角分页图集和界面图、海军本部舞台、技能图集、音频清单全部60份采样，以及显式旧版选项使用的图集。
- 首页在模块执行前补齐完整动漫默认参数；显式 `art=legacy` 和性能档参数保留。游戏模块、素材内容与已验收版本逐字节一致。
- 原始生成图、失败候选、源语音包、source maps、旧编译脚本、凭证和验收录像均不进入部署包。
- 包和复制清单默认保存到 `.local-releases/公开部署-0914/<时间>/`；本轮因 C 盘 ENOSPC，显式使用 `D:/one-piece-fighter-pages-0914/2026-09-14T10-20-59-599Z/`。逐项校验 SHA256 后再推送，保留原构建和各次打包结果。本轮新生成的临时验证构建已移至该 D 盘目录的 `build-check/`，407份文件移动前后哈希一致。

## 更新方法

1. 本机完成需要发布的构建验收，再运行 `node scripts/packagePages.mjs`。指定暂存位置用 `node scripts/packagePages.mjs .local-releases/candidate-0913 D:/one-piece-fighter-pages-0914`。
2. 从输出的 `site/` 按项目子路径启动本地静态预览，验证默认动漫入口与资源加载。
3. 首次发布将 `site/` 建成独立 `gh-pages` Git 分支；后续获取远程 `gh-pages` 历史后追加提交，不强推或重写历史。
4. GitHub Pages source 指向 `gh-pages` 根目录。构建成功后验证公网无缓存首次加载、标题→选人→正常比赛、声音采样和暂停。
5. 保存 Pages 构建、提交、文件哈希、网络与浏览器实际结果。HTTP 200 或 Pages 构建成功不能单独代表可玩。

## 本轮结果

- 仓库已公开。Pages 使用 `gh-pages` 根目录，HTTPS 已启用，GitHub 构建状态 `built`，错误为空。
- 发布提交：`9b5d046f84630c5b4edc5f0b45dbc10e4c25e77c`。
- 运行包91份文件、42,851,624字节；其中89份网页/资源逐个通过公网 HTTP 200 和 SHA256 一致性检查，其余2份是 `.nojekyll` 与 `.gitattributes` 元数据。
- `typecheck`、`lint`、51文件564项测试、独立输出目录的 `build` 全部通过；打包脚本补充改动后单独 lint 通过。游戏模块与原交付的 `index-Crbo9vzy.js` 完全一致。
- 本机项目子路径和 GitHub 公网根链接均默认进入 `anime/full/high`，两角动漫加载与必需舞台/FX结果均为 `ok`，没有资源失败。性能档、显式旧版和显式内部样板参数不会被默认入口覆盖。
- Edge 公网首次加载的80项资源请求全部200且有传输字节；最大赤犬PNG下载约2.21秒。全部60份音频下载并解码成功，用户按键后 AudioContext 为 `running`、声音状态 `ready`，无音频资源错误。该耗时仅代表本机当次网络，不能保证其他地区访问速度。
- 公网真实键盘流程已完成标题→菜单→路飞对普通CPU赤犬→正常掉血/KO→整场0:2结算；交战记录已出现183次采样播放，音频资源错误0。暂停状态下活动声音为0。画面包括新人物、海军本部、有限血气与当前战斗技能，未出现旧图自动回退。
- 结算后按 J 再战，重新进入完整动漫普通CPU比赛，新局正常推进与掉血；采样累计325次，失败音频0。Enter 暂停后活动声音0，再经菜单回标题仍为 `anime/full/high`，声音就绪且无残留播放。焦点模拟和短时画面采集已关闭，没有留下浏览器测试开关。
- 后台窗口最初约1fps，短时浏览器画面采集与焦点测试后正常推进。本次仅将其作为公网流程核验，不新增性能达标结论，也不把短时采集当成15分钟性能测试或带声比赛录像；原交付的独立浏览器性能证据仍单独保留。

发布包目录内保留 `copy-before.json`、`package-report.json`、`package-verification.json`、`public-http-verification.json`、`live-first-load.json`、`live-combat.json/png`、`live-input.json`（整场结束状态）、`live-result.png`、`live-rematch.json`、`live-final.json`、`github-pages.json` 和 `github-pages-build.json`，不随网站公开。
