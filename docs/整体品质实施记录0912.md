# 整体品质实施记录 0912

用户已批准全力执行 `整体品质升级计划0912.md`。本记录只写实际成果，不把开工、候选图或测试通过当作样板验收。

## 用户试玩反馈与定向复审

2026-09-12，用户反馈人物仍像像素风、声音好像没有，技能连贯性“还可以”；并给出截图确认点击的是助手提供的新版样板链接。该截图没有展示打开后的游戏画面或运行状态，不能认定用户选错入口，也不能凭旧成功截图否定本次反馈。样板未获整体美术/听感放行，暂不扩产。

本次只读核验：当前4176服务及两角anime资源均200，构建身份未变；原画已被离线缩成低分辨率运行裁片，再在960×540画布中显示。存在“新图精细度损失”和“新图加载/校验失败后整对回退旧图”两条不同分支，尚缺用户当前页面状态来判别。51份音效文件存在且非静音，正式voice映射仍0；持久化音量与音频上下文等原因也待现场检查。浏览器工具本次因kernel assets路径错误无法读取用户标签，未擅自改设置或把旧测试状态当现场。

已按用户要求整理手动上传Pro的复审包与可复制问题，位置为`.local-releases/网页Pro复审0912/`，说明见`网页Pro复审说明0912.md`。目的为基于真实材料的定向诊断，不重复宏观重规划；没有自动上传，也没有修改候选代码、素材或战斗数值。包内明确区分用户入口图、过去新图成功截图、受控回退截图、音视频可读取性及当前未知项。

## 基线与当前分工

- 旧 `dist/` 已复制到 `.local-releases/baseline-0912/dist/`，文件 SHA256 清单为同目录上一级 `dist-sha256.json`。
- 当时的源码、脚本、测试、文档、本地素材和项目配置已复制到 `.local-releases/baseline-0912/workspace/`。不覆盖既有文件、不删除旧成果。
- 新候选构建输出 `.local-releases/candidate-0912/`；默认 `dist/` 暂不覆盖。
- 主任务：官方 Image 2.5 网页生成、原图检查、同屏风格、限定动作样板与场景接入。
- 动画管线：显式连续帧、逻辑帧停留、根点与连接点、确定性透明处理/图集、纯逻辑测试。
- 真实音频：已下载声音的切片、来源与时间段、采样清单；不能用 ASR 或播放命令冒充实际听感验收。
- 音频机制：阶段 cue、去重、声音实例与缓存、暂停/恢复/退出清理。

## 当前成果与未验收边界

路飞伸臂与赤犬大喷火双机制样板已合并到`.local-releases/candidate-0912/`，当前页面引用`assets/index-DJP7zWxv.js`。根目录`体验新版样板.cmd`启动，已运行时可打开`http://127.0.0.1:4176/?art=anime&mode=training`。这是限定动作训练场：普通完整比赛仍使用保留的旧版入口。

两角新人物原画、路飞短臂回收及击飞/倒地/起身、大喷火同手连接已完成指定官方网页生成、来源留档、透明处理、显式时序与候选图集接入。大喷火通过已有熔岩中段的有限离线配准贴合原攻击前缘，未修改战斗数据。路飞短臂回收将相邻拳前缘的最大跳变从196.91降到80.10屏幕像素，但这只是几何改善，不证明主观动画流畅。

正反朝向真实键盘功能检查、资源缺失回退、录像解码和短时资源观察已有证据，见`.local-releases/second-sample-0912/验收结果0912.md`。实际目检覆盖截图与视频取帧图条；正常速度的主观动作观感、听感和用户样板放行尚未完成。跟步滑行、近身长臂越过对手、回收衔接及熔岩中段纹理拉长仍是明确的视觉限制，不建议据功能通过直接批量扩产。

正式人物语音仍为0条，36条候选待实际听辨；完整新人物动作与技能、完整新比赛、十五分钟运行、真人复玩与真实手柄均未完成。按已批准计划第6节集中审阅两类样板后再决定扩产；本轮未替换默认正式人物或旧dist。

## 已完成的工程与音频检查

- 新候选显式动作时序、根点/连接点、有限输入白名单、整对回退、HUD与完整舞台构图已接入；保留原MoveData，不为原画改判定或时长。
- 原图、提示词、型号凭据逐文件SHA256绑定；运行时配置/图集/PNG按完整内容哈希登记纹理，避免新配置误用旧图。仍未覆盖默认dist。
- 历史检查（2026-09-12 01:45）：typecheck、lint、Vitest通过，32文件305测试；当时build与素材闭环尚未完成。最新候选检查见下方“第二机制收尾”。
- 同版本核心共同合法输入重放：基础1120帧；包括大喷火击飞/起身共1340帧一致。不是完整招式表与限定招式表任意输入等价。
- 开发入口独立Edge音频观察：第一次真实J之前51采样已下载、解码0；按键之后51采样已解码，设备ready；暂停/静音清空播放，F6仅重启环境声，经暂停菜单退出清空播放。
- 正式人物voice映射仍为0；36条语音候选待听辨，不能把音频管线通过当人物语音已完成。

## 伸臂试制的方法调整

首稿臂长不足、袖口消失；定向返工修复身体连接但射程过长且拳位偏低。没有继续第三轮同类批量生图，也没有改变战斗判定。

受控局部几何校准已纳入显式离线管线：直橡胶段压至51.8881%，抬腕8.6837度，固定袖口边界；保留原拳形与身体比例。拳心为屏幕坐标`[292.13,-140]`、前缘`308`，对应st_c原判定；身体/脚/肩/袖保护区逐像素变化0、袖口到拳头透明度连通。29逻辑帧保持9起手、4有效、16收招。后续增加三张指定模型生成的短回收原画，配合三段显式已有橡胶像素压缩，改善回收突跳；不能用几何通过替代动作验收。

## 首样板的实际检查与继续工作

- 首样板历史全量检查：33文件315测试，以及typecheck、lint通过；当时隔离候选build成功，旧dist37个文件与基线哈希一致。当时两角图集各1024×1024；第二机制扩充后均为1024×2048。
- 首样板录像的第一份导出因data URL分割错误无效，已重录并用FFmpeg解码/音轨检测核实；有效34份证据逐文件SHA256保存在first-sample目录，没有交付失败录像。
- 当前新版入口为根目录`体验新版样板.cmd`，只启动已构建的candidate；默认`开始游戏.cmd`仍启动旧正式版。候选服务按完整文件身份校验，重建后必须重启，避免看错版本。
- 实际键位提示已补，支持重绑与朝向；连续大喷火可打出KO的样板边界已修：原core无限血只在安全时恢复，不等于不会死亡。样板使用既有重置方法在完整攻防结束后恢复，并在致命状态被表现消费前兜底重置，不渲染未覆盖的KO/胜利。新增大喷火木桩按真实236+A指令出招，不直接改角色状态。工程模拟以及第二机制浏览器连续出招检查均已通过。

## 第二机制收尾

- 最新候选四项检查全部通过：`typecheck`、`lint`、`test`（35文件333项）、隔离输出的`build`。冻结记录为`.local-releases/second-sample-0912/构建与检查0912.json`；13个代码/配置/图集文件再次SHA256核对一致，页面返回200且引用最新脚本。
- 与0912开工快照比较，24个core/characters/input文件和旧dist的37个文件保持相同；候选只放独立目录。两角图集各1024×2048，RGBA8基础纹理各约8MiB，不以PNG压缩大小代表显存。
- 独立Edge浏览器由脚本发送真实键盘输入，覆盖236+A空挥/命中、后退格挡、霸体窗口结束后的轻拳打断、击飞→倒地→起身、空中攻击键受身、真实移动到墙角后的墙弹、连续施法和暂停/重置。暂停中的F6立即重绘idle及复位位置，逻辑仍暂停、声音仍暂停。
- 缺少atlas中的hit_air帧会整对回退旧人物并关闭大喷火；合法runtime不声明hit_air时保留基础新人物，仅关闭该能力小样。两种情况分别有独立记录，不混称一种回退。
- 正向/镜像原始录像分别约42.33/48.80秒，FFmpeg全量解码成功，含Opus游戏音轨；正常时间轴的MP4片段另存。截图及图条目检可说明身体连接和姿势变化，不能替代正常速度主观观感与实际听辨。
- 无录制、无截图、同时不运行FFmpeg的25秒采样：逻辑1500步；Phaser poststep回调间隔p50/p95/p99为4.8/5.3/5.6ms，最大7.6ms，5294次采样中无大于33ms间隔。该指标不是显示器实际呈现FPS，不能以此宣称稳定200FPS或最终60FPS验收。
- 同窗纹理33→33且尺寸列表一致，51采样均解码，采样播放4→70、合成回退0，采样时活动声音最大4、技能精灵池最大1。只是短时观察，不代替完整技能密集场景与十五分钟稳定性。
- 用户本轮的免审批权限已由新子任务继承；旧受限子任务不再派发。日常检索、下载、构建和验证均不带权限升级参数，具体排查见`终端权限排查0912.md`。
