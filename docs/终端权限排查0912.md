# 终端权限排查 0912

2026-09-12 用户反馈已开完全访问但仍出现“允许一次”。读取当前任务与三个旧子任务的最新 turn_context 和执行记录确认：

| 执行上下文 | 实际策略 | 结果 |
|---|---|---|
| 当前主任务 | danger-full-access / never，网络开启 | 普通终端命令直接完成 |
| 旧 combat_audit、presentation_audit、sprite_scout | workspace-write / on-request，网络受限 | 旧子任务仍提交 require_escalated，触发审批 |

原因是旧子任务保留了创建时的权限上下文；主任务切换权限后，继续向旧子任务发消息并没有刷新它们的有效策略。不是用户没有授予权限。

处理：保持三个旧子任务中断，不再派发命令；新建 audio_finish、audio_stage_finish、av_acceptance，明确分工并承接现有文件。三个新子任务均报告实际 danger-full-access / never，并分别通过无覆盖参数的 node --version 或 npm run typecheck 验证。主任务和新子任务的常规命令不再提交 sandbox_permissions、require_escalated、justification 或 prefix_rule。

没有修改系统配置或账号权限，也没有删除旧任务/成果。当前授权范围内的常规检索、下载、构建与测试无需逐条申请。用户先要求重新规划，随后明确“现在开始全力执行计划”；主任务和新子任务已按新的授权继续实施，常规命令均未提交权限升级参数。旧受限子任务仍不派发工作。
