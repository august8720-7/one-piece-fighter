# 浏览器与性能验收0913

## 当前状态（后续实施）

2026-09-14 完整动漫新版已完成本机内部交付：开始游戏.cmd 默认进入新标题，开始旧版游戏.cmd 保留旧dist。当前游戏主脚本 index-Crbo9vzy.js，两角各25个基础状态、合计53招有合法新图；通用出力/受击/KO/胜利原声8类9段已接入。独立Edge正常速度持续交战900秒，逻辑60.0004Hz，超过50ms浏览器回调间隔0次，纹理起止31→31、音频缓存60→60。Windows启动网址参数截断及过早结束交接已修复，并从实际按钮完成标题→选人→有限血气CPU→返回标题。typecheck、lint、51文件564项test及build通过。真人最终听感、美术喜好、复玩评价和普通CPU被重复长拳利用的风险仍保留。直接使用见 docs/完整游戏交付0913.md，证据见 docs/High收尾实施与验收0913.md。

以下保留各轮检查与历史状态；旧的语音0条及等待上午监测的描述不代表本次候选。

2026-09-14独立复核更新：最新完整动漫候选已完成一次900.003秒有窗口Edge监测，但这次**不能通过流畅60Hz验收**。实际交战仅139.865秒，且仍有长停顿；持续运行、资源回落、音频通路与性能结论分别记录。下文10:02和早间样板记录保留为历史，不用旧结果代替新版本。

## 10:41—10:56完整动漫候选十五分钟监测（流畅性未通过）

原始证据：`.local-releases/改造验收-0913/独立Edge十五分钟监测最终候选0913.json`，SHA256 `46f51e431b0968b74948a3fde04604ca0db8578ab44bb3d551274f0cf2b8c1f9`。北京时间2026-09-14 10:41:20.533—10:56:20.538，监测墙钟900003ms，自行到时结束。起止脚本均为`index-CVDYMAcE.js`，`anime/high/full`，1920×1080显示及画布，两角色`textureDensity=2`，人物纹理身份不变。人物、海军本部两层及两角色FX加载结果均为成功。

| 检查 | 本次实际结果 | 结论边界 |
|---|---|---|
| 时间组成 | 交战139.865秒，占15.54%；菜单349.716秒、暂停300.122秒、比赛结束等待90.793秒；其余为开局、KO慢镜与回合结束 | 完成十五分钟监测，不是十五分钟持续交战 |
| 交战RAF | 15295个间隔；p50约6.7ms、p95为10ms、p99为30ms；超过50/100/250ms分别105/84/30次，最大1010ms | 平均109.389次回调/秒不能掩盖长停顿，也不是物理显示帧率；流畅性未通过 |
| 交战逻辑 | 6916帧÷139.7593秒＝49.485Hz；暂停记录0帧；KO慢镜16.854Hz单列 | 保留60Hz设计，但本轮实测墙钟推进率未达标；不能用约150Hz的局部RAF替代逻辑验收 |
| 实例与资源 | 3个sim实例，负帧重置0；181个资源采样点，场景始终8；纹理18—43、起止19→19 | 菜单/暂停界面会改变纹理数，不能把峰值当成持续增长 |
| JS已用堆 | 62,322,389—126,122,546字节，起止64,614,191→64,377,354字节 | 退出后有回落；无强制GC、堆快照和足够连续密集时长，不证明绝对无泄漏 |
| 播放与投射物 | 每5秒采样播放实例0—4，暂停与菜单均0，最终0；投射物0—3、最终0；缓存音频始终51、pending始终0 | 这些是间隔采样，不表示捕获了瞬时最高负载 |
| 音频实际播放 | 累计339次采样、19次合成回退；recent归因338/18，各漏1次 | 不能写“零回退”或“零漏采”；不代表正式日语语音已取得 |

逻辑Hz使用**同一sim、同一类别的相邻poststep帧差除以墙钟间隔**。绑定首帧、换sim及负帧重置不会产生跨比赛帧差；两次回调之间没有回调的时间仍计入分母，不能为了提高Hz而剔除。5秒资源点独立复算也出现80.121—85.122秒约28.60Hz、90.122—95.124秒约0.60Hz、415.578—420.579秒约36.19Hz、600.646—605.647秒约36.00Hz，同时有多个窗口接近60Hz。

暂停、KO慢镜、开局、比赛结束和场景切换单列，不作为普通交战性能样本。本次没有记录到后台/失焦类别，不能据此推断这些长停顿来自后台。`errors=[]`是监测器自身错误数组为空，不是整个页面控制台错误数为零。源纹理RGBA基底估值起止206,421,044字节，来自尺寸乘4，**不是GPU内存实测**；场景顶层子对象统计也不涵盖所有嵌套显示对象。

音频已归因的18次合成均为`menu_move`8次、`menu_confirm`6次、`round_start`4次；这三个cue当前没有采样覆盖，使用默认合成。剩余1次合成未取得cue记录，不能擅自归类。所采丢弃事件1481条中，重复事件1410条、环境循环已播放68条、冷却3条；不等同于下载或解码失败。音频诊断被其他页面操作关闭3次，累计计数揭示各1条采样/合成未归因，不能因`suspectedWindowGaps=0`就写零漏采。起止无失败样本、音量设置未变，主音量后峰值0.365328、4020个采样点非零，只证明音频输出通路；正式日语人物voice cue仍为0，用户扬声器听感另验。监测结束后诊断恢复原false，监听、RAF和定时器清理由监测结果报告。

### 长停顿原因仍须隔离复验

首轮汇总没有保留逐次长RAF的时间戳，因此不能精确对齐录屏、截图、大CDP对象或base64取回。并行文件`教学转路飞普通完整比赛0913.json`记录录像在本监测408372ms时开始、471035ms时请求停止、停止后导出约151ms。415.6—420.6秒等低速窗口早于停录导出，故不能把全部问题归因base64取回；持续编码或其他操作是否贡献也尚无分离实验。文件写入时间不能充当操作起止时间。

下一次使用独立轻量监测：不启用音频诊断、不收recent、不录屏、不截图，5秒仅读资源标量，保留实际RAF、逻辑及带墙钟区间的长帧记录，结束后一次取回结果。窗口保持前台，在输入驱动和训练场就绪后开始连续密集技能；不得用菜单或暂停凑满交战时长。此处保留原始未达标结果，不在新结果产生前宣称完整性能验收通过。

## 10:02全覆盖候选与当时待验项（历史快照）

2026-09-14 10:02更新：两角全部基础状态与招式现已达到结构与来源覆盖门槛，完整动漫游戏仍未验收。本文把最新全覆盖候选、此前样板实测和最终待办分开记录，历史测试不会因素材补齐自动升级为最终版本证据。

- 作者清单：`75d271d08525d91eff989e5dde7661f290bb44fabab67aedb7d6249c97d9b18c`；09:47覆盖报告为两角各25/25状态、路飞27/27招、赤犬26/26招，`productionCoverageGateReady=true`，无无效项。对应`docs/全动作技能覆盖0913.md`及既定验收目录的同名JSON。
- 本批工程：主任务已完成`typecheck`、`lint`、50文件547项测试和退出码0的`build`。候选主脚本为`assets/index-7VcTSl1e.js`，4177服务已重启；本次文档同步已读回候选脚本名，但新构建浏览器重新加载、实际纹理、声音输出及整场比赛仍待验证。
- 入口：`体验新版样板.cmd`仍打开`?art=anime&mode=training`，默认`开始游戏.cmd`仍服务旧完整`dist/`。全覆盖候选的完整标题应使用`?art=anime&scope=full`验证；素材门槛通过不等于默认入口已经切换，也不需要删掉合法的训练或样板分支。
- 画面边界：四姿dash内部比较采用`[6,3,3,4]`，尚需真实游戏跑停接招；`j_cd`早出站防掌偏高仍待改。断腕的`9b62b83d…`局部补片拒收，现有合法素材保留。正常速度离线比较只证明所看片段，不证明浏览器显示、操作或声音。
- 声音边界：正式日语人物语音仍0条，36个候选未听辨。旧浏览器非零音轨证明当时采样通路输出，不能当作已接入日语语音或用户实际听见。
- 战斗冻结：29个核心、角色、输入与AI文件与两份近期构建前快照逐字节一致；已有16场60,498帧同输入回放及接触坐标修复记录已重新核对文件、逐场输入和当前源代码哈希。本次未重跑回放，不能据此替代新版浏览器对局。

| 最终验收项目 | 本次状态与必要证据 |
|---|---|
| 新构建加载与资源异常恢复 | 待浏览器重新加载并核实构建、实际人物纹理和画布；旧故障矩阵保留为历史 |
| 真实动漫完整比赛 | 待两角各自胜负、结算、重赛与重选；本地双人、训练、教学实际完成后转人机及设置返回 |
| 动作与接触 | 待跑停接招、左右朝向、近中远距离、早中晚空中接触与回收；修复已知双掌偏高 |
| 声音与语音 | 待正式语音听辨、混音及完整对局播放/暂停/恢复/退出核验；不把非零波形当作听感 |
| 独立桌面连续15分钟 | 待最终新构建密集技能和反复重赛，分别记录浏览器帧间隔、逻辑60Hz与资源变化 |
| 交付与入口 | 待同尺寸正常速度带声前后对照、完整比赛记录、最终素材/声音覆盖清单；验证后切换默认新入口并保留明确旧版 |

以下章节均保留对应历史构建的证据。“最终”若出现在旧记录中，仅指该次样板收口，不指本轮完整动漫游戏。

## 0913早间样板构建和工程检查（历史）

当前候选入口：`体验新版样板.cmd` → `scripts/playCandidate.mjs --release candidate-0913` → `http://127.0.0.1:4177/?art=anime&mode=training`。

0913 02:49样板收口后重新执行四项检查全部通过：typecheck、lint、42个Vitest文件共391项测试、build。构建使用`npm run build -- --outDir .local-releases/candidate-0913 --no-emptyOutDir`，当时主脚本`assets/index-6Hbh6Y9c.js`，没有覆盖旧`dist/`。

日志在 `.local-releases/改造验收-0913/engineering-*-final0913.txt`。完整新人物覆盖另行检查，路飞 11/25 基础状态、2/27 招式，赤犬 8/25、2/26；该检查不通过，不能混同为工程单测失败或用单测全绿覆盖它。

## 加载、声音与画面

`index-ozunHDNw.js`构建在独立有窗口 Edge、1920×1080 显示尺寸下完成 12 项检查，全部通过，页面异常 0：

1. 人物资源 404、非法配置、缺帧、8 秒超时均阻止战斗创建；恢复资源并重试可进入动漫样板。
2. 请求完整动漫比赛但缺动作时保持阻止状态，不转入旧人物或受限训练。
3. 只有点击主动进入旧版才采用旧人物。
4. 保存静音且总音量为零时，开启声音只解除静音，零音量保留；主动恢复默认和试听有效。
5. 采样 404 后重试成功；HTTP200但音频内容损坏时明确解码失败，替换为正常文件后重试实际重新下载并解码成功。
6. 暂停菜单试听通过与战斗相同的音效组及总音量，总音量后峰值实测非零；退出设置保持动漫身份。
7. 完整覆盖不足的主提示和按需诊断分别截图检查，没有原先长列表越界。
8. 实际原生 requestAnimationFrame 及逻辑更新采样，见后文。

结果与截图在 `.local-releases/改造验收-0913/browser-final-1920-1789237299797/`。`previewPeak=0.26210418343544`的信号峰值仅证明游戏输出链路，不证明系统扬声器、耳机或用户听见。原用户故障页的音量、输出设备和加载状态仍未知。

1280×720 同尺寸的四组密度/画布对照另在 `density/`。两类显示尺寸均打开实际PNG检查过；不以AI复审文档中的离线实验冒充本机浏览器结果。

## 历史样板十五分钟持续运行

文件：`.local-releases/改造验收-0913/endurance-1789236230652/report.json`。时间：02:03:50–02:18:53；实际战斗采样窗口 900331ms。该轮使用接触坐标与损坏字节重试最终修复之前的冻结脚本 `index-CwdwkOnr.js`，身份 `eb24c3586138554e87bc5a62fc4e9100f53e5fb208862ee338dd4b7b22555f95`。

- 独立有窗口 Edge，1280×720 显示，1920×1080 动漫画布；实际键盘移动与轻重拳，对手使用内置大喷火木桩，F6重置5次。
- 54006 次逻辑步进，573 次实际命中或格挡；逻辑平均约60Hz。
- 270033 次 Phaser poststep 回调，最长9.5ms，超过50ms的回调间隔0次；页面异常0、缺帧0、失败采样0。
- 纹理数27–28、活动投射特效精灵池最终2、残影池24、音频播放实例每分钟采样1–5。池大小保持有界，未观察到持续增加的纹理或残影。
- JS已用堆的每分钟采样在9.5–30.1MB之间波动，末尾23.6MB；初始7.5MB尚未充分预热。没有强制垃圾回收或浏览器堆快照，因此这些数据不能证明绝对无泄漏。整场重赛的资源清理需另验。

Phaser回调频率不是显示器实际呈现帧率。本机随后在后续构建 `index-ozunHDNw.js` 做了10001.7ms原生RAF采样：页面可见且有焦点、未使用setTimeout替代RAF，3001次回调，中位间隔3.3ms、95分位3.4ms、最大4.7ms；逻辑步进600次。这里只报告浏览器调度与逻辑时间，没有把约300Hz的回调说成“显示器300帧”或用无界面循环冒充显示性能。

十五分钟结果只证明上述冻结样板的持续运行。当前已达到全动作结构覆盖，最终完整动漫版本的十五分钟、密集全技能及反复重赛资源验收仍须在新构建执行。后续局部修复的定向验证仍保留其实际构建身份，不把旧构建900秒记录改名为最新构建记录。

## 回退检查

02:23重新按改造前清单核对旧dist37个文件、0912候选312个文件，共349个文件SHA256完全一致。记录为 `.local-releases/改造验收-0913/rollback-verification0913.json`。旧完整入口 `开始游戏.cmd` 保持原样；新版样板按钮现已明确指向0913候选。

## 历史旧图完整流程与视听媒体

候选 `index-ozunHDNw.js` 已完成两角各一次胜利、一次失败，共4场完整旧图人机比赛，均通过结算、重赛、重新选人；本地双人双方实际命中，教学完成实际格挡、特殊技和二连后进入有限血气CPU。6段实际canvas+主音频通路视频已完整解码，无页面异常。证据在 `.local-releases/改造验收-0913/full-flow-1789237482667/完整流程验收0913.md`。动漫完整比赛仍受动作覆盖门槛阻止，不用旧图流程通过代替。

后续已将这6段与最终教学共7段原文件逐帧核对时间戳：路飞负场、赤犬胜负场及首轮教学可按原时间轴观看；路飞胜场、最终教学的片头各有约1.1秒采集缺帧，双人短片缺少足够的音画起止对齐记录，这3段仅作流程证据。原文件未改速、未重录；解码通过及文件名中的normal-speed均不当作速度认证。两批目录内的`录像时间戳复核0913.md/json`保存具体边界及逐帧记录。

## 历史样板收口修复与定向复核

实际看图发现经典舞台在KO推向边缘时露出纯色竖条、教学完成卡与暂停标题重叠。现按图片实际宽度夹紧经典视差位移，并在完成教学时收起旧卡；动漫背景既有满屏构图保持。原异常截图保留，定向复核另记。

赤犬受击手套的亮绿边确认来自绿底残留。最终仅该帧启用显式3像素连通去绿和三个指缝白名单：强绿像素268→0，其余52个运行帧像素完全相同，原图/根点/连接点/帧矩形/时序均不变，56113个非绿色源像素SHA一致。最终记录 `density/green-edge-opt-in-final-verification0913.json`，中间全局参数试验不作为最终采用方案。

最终构建 `index-6Hbh6Y9c.js` 已在真实Edge完成3种试听退出：标题等待下载时离开、设置等待下载时离开、设置试听已播放时离开。等待时没有退出后新增的采样播放，正在播放的试听立即停止，无页面异常。证据 `.local-releases/改造验收-0913/preview-exit-1789239466282/report.json`。前两种使用明确的旧图冷启动入口以确保文件真的未预加载；最后一种使用动漫入口。此前一次脚本用已预加载动漫入口误当冷下载，另一次误要求已关闭诊断仍保留日志，均为测试条件错误，失败记录保留；修正条件后才计入上述3项结果。

最后构建的左右舞台边缘、教学单标题及10次场景往返资源检查已通过，结果在 `.local-releases/改造验收-0913/final-regression-1789239512491/`。左右真实键盘行走至墙角，背景实际覆盖全画布，截图已打开检查；教学完成仅1个可见标题且进入正常CPU。10次同页面暂停→选人→重新开战后，纹理保持32、活动子对象保持36、pending保持0；暂停playing=0、恢复恰好1条环境声，场景监听未累积。没有重新把已通过的四场旧图比赛当作新人物完整比赛。

## 历史样板两招视听对照

[对照页](../.local-releases/改造验收-0913/normal-speed-1789240081284/现有两招视听对照0913.html)比较路飞伸缩重拳和赤犬大喷火的左右朝向。新4段来自最终`index-6Hbh6Y9c.js`；旧4段来自保留的`index-DJP7zWxv.js`，其中3段按实际文件引用`normal-speed-1789237369907/`，不复制为新构建证据。8段原始WebM与MP4的SHA均匹配索引，所有播放路径实际返回HTTP200。

固定`captureStream(60)`记录真实键盘出招和游戏总音量后的音轨，未改速。8段视频末时间戳约3.55–3.70秒，音轨结束约3.75–3.94秒，录制及停止封装墙钟约3.77–3.93秒；音频末尾与墙钟最大绝对差约30毫秒。逐段值见`最终视听媒体索引0913.json`。视频帧间隔中位数约17毫秒，但最大间隔213–283毫秒，不能把目标60帧采集率当成每帧都已连续录到，更不能据此推断显示器实际帧率。

实际PNG已查看，最终新图的受击绿边清除，火花位于接触处；近身长臂越过对手、回收较硬和熔岩中段拉伸仍保留为待解决项。两招页面另以本地无界面浏览器验证4组选项的双边媒体加载、1倍速播放、相同显示宽度和0页面错误，仅证明媒体页可用，不计入游戏实机性能或真人听感。页面测试及逐文件核对见同目录`对照页实测0913.json`、`媒体路径与哈希复核0913.json`。
