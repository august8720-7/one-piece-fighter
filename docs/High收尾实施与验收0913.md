# High收尾实施与验收0913

2026-09-14，按用户决定以 High 完成此次内部收尾。当前游戏构建 `index-Crbo9vzy.js`，`开始游戏.cmd` 已切换到完整动漫标题，旧版由 `开始旧版游戏.cmd` 保留。本文是本轮最新记录，先前候选与失败媒体均保留；真人最终听感、美术喜好及复玩评价不由工程检查代替。

## 已落实

- 两角各接入通用出力、受击、KO、胜利，共8类、9个短片段。角色声线来自用户实际试听确认；事件用途是制作选择，不认证逐字台词、招名或全部库存。来源、区间和SHA256见 `scripts/voice_manifest.json`，核验与合并脚本为 `scripts/apply_voice_manifest.py`；音频重制调用同一函数，不再清掉正式映射。
- 渲染上限120，保留原Phaser时间平滑及60Hz逻辑。已安装Phaser的时间戳实验显示limit60在部分刷新率下会低于60次渲染；limit120的已测59.94/60/75/90/120/144/165/207/240/360Hz输入下，渲染回调约59.95至103.5次/秒。这个实验是内存时间戳模拟，不代表实机帧率。
- 修复内置Chromium的大图读取路径：赤犬15,668,551字节PNG用 `response.blob()` 在本机失败，同URL用 `arrayBuffer()` 再创建Blob成功解码为4096×4096；并发与顺序原始字节下载均成功。现先读取原始字节、计算原有SHA256、再创建解码对象，减少一次Blob转回字节。浏览器内部为何流式Blob失败仍未知。纯网络失败最多重试一次，共用原8秒截止；HTTP失败、图片损坏与最终失败仍报错，不回用旧人物。
- typecheck、lint、50文件561项测试和build通过。新增覆盖真实运行清单语音映射、PNG请求/正文中断、持续失败、404，以及Blob流式读取失败时保持原图身份和对象URL清理。
- 战斗与AI相关29份源码哈希与上一轮一致，见 `语音限频后战斗冻结检查0913.json`；本次不是新回放实验。

## 实机证据及边界

以下文件均位于 `.local-releases/改造验收-0913/`。

| 记录 | 实际证明 |
|---|---|
| `语音清单完整性检查0913.json` | 重复合并不变；错误哈希、错角色路径、未确认专属招名拒绝 |
| `双角色技能语音实机0913.json` | 60.0006秒，10组重点技能均实际出现并命中；两角出力和受击声音有真实事件、播放及非零输出电平；无遗漏中间逻辑帧。使用合成DOM键盘输入，无核心状态改写 |
| `语音限频内置浏览器短测0913.json` | 90秒短测，首轮木桩未切human，路飞侧未形成出招，不能当成双角色验收；约59.05Hz逻辑，仍有两次约1秒级间隔，不能通过持续60帧验收 |
| `语音接入后重赛传输失败0913.json` | 首场路飞胜利，赤犬受击/KO和路飞胜利声音真实触发；随后重赛大图传输失败。配套webm因记录脚本错误仅7字节，不能播放，不作为视频证据 |
| `语音与大图修复后三场比赛0913.json` | 修复后的三次结算、两次重赛成功；第二、三场均从frame0/intro、满血、0:0开始。首场从frame2452开始，不能称从开局完整记录。结算播放实例为0。配套webm同样导出失败，保留且不计作有效录像 |
| `大图Blob读取故障定位0913.json` | 同一PNG的Blob流式读取失败与完整原始字节读取/解码成功的对照 |
| `双方KO胜利与重赛原速带声0913.webm/json` | 修复后的有效录像：约120.09秒，1920×1080、VP9视频和48kHz立体声Opus；双方hurt/ko/win均由真实事件播放。首场从frame1390开始，第二场从frame0/intro完整进行；有限血气和正常比赛速度。文件71,738,304字节，SHA256为51b90a11c09c6ad80fbbdd7c59672c87540c77abc71069fe0ed5206e98ecb21e |
| `双方声音录像解码核验0913.json` | 全片解码退出0；解码7187帧，媒体时间约120.07秒。ffmpeg打印的1k tbr是时间戳估计，不能当作真实1000FPS。录像每场轮流一侧进攻、另一侧不反击，用于流程验证，不证明CPU强度或好玩 |
| `正式语音暂停静音实机0913.json` | 真实Enter暂停时无播放实例；恢复仅重启环境声。真实M静音后无实例，再按M恢复原0.6总音量与各分组音量；没有改系统音量 |

早期资源加载失败均明确停止战斗，未出现静默旧图回退。读取方式修复后首次加载、三次结算和两次重赛已成功。记录脚本原先对含 `vp9,opus` 的data URL按逗号分割，误取MIME片段，已改用无codec后缀的Blob导出；另外战斗退出会关闭声音诊断，复录在下一场重新启用诊断。它们是记录工具问题，原始事件/结算记录有效，但前两份7字节webm无效。双方声音的新录像已分段导出、校验哈希并完整解码，通过实际画布截图检查。录像不是麦克风录音或用户实际听见的证明。

有效录制期间总线抽样峰值约0.493，观察到247次采样播放；另有4次既定合成菜单/开局提示，不是人物合成语音。结算时播放实例0；重赛资源重新校验成功。录音连接、媒体轨道、驱动按键、监听器和定时器均已清理，原诊断开关已恢复。

## 17:31—17:46 独立 Edge 十五分钟持续交战：通过

扩展弹窗阻塞已解除。用户同意保持独立 Edge 在最前面，准备后已明确告知开始。正式区间为北京时间2026-09-14 17:31:06.400—17:46:06.401，900000毫秒全部为同一训练场的正常速度交战，1920×1080画布、双倍密度人物、完整动漫配置。正式区间不录屏、不截图、不构建、不跑测试、不启用音频诊断；结束后一次取回完整结果。输入通过既有键盘通路，没有改写核心状态。

| 指标 | 本次结果 |
|---|---|
| 逻辑推进 | 54000逻辑帧，60.0004067Hz |
| 游戏绘制回调 | 89993次，约99.992次/秒；这是Phaser回调，不是物理屏幕FPS |
| 浏览器RAF | 约299.975次/秒；p99 3.5ms，最大9.9ms；超过25/50/100ms均0次 |
| 游戏单帧CPU区间 | prestep至postrender平均0.399ms，p99 3ms，最大11.8ms；不包含GPU完成和显示呈现 |
| 主线程长任务 | API可用，观测0次 |
| 资源 | 场景始终8；纹理起止31→31、期间31—32；音频缓存始终60 |
| 已用JS堆 | 起止70,602,570→72,544,010字节；最低64,010,642、峰值97,348,669，有92次采样回落；不是GPU内存 |
| 声音 | 2197次新增采样播放，0次新增合成回退；5秒采样的同时播放实例1—7 |
| 技能与清理 | 10组重点技能18轮完整循环，三发火山57枚、八发火山152枚；无观测漏帧或错误，结束后松键、移除监听和定时器 |

原始文件 `High独立Edge十五分钟通过0913.json` 的SHA256为 `5cbb51b05bf08d7632f7ea880a9cdcef45c1ef1237ea866abca09cc785edf7c3`，详细派生指标在 `High独立Edge十五分钟汇总0913.json`。旧测试383次超过100ms的记录完整保留；本次为0，但两次RAF频率及运行条件不同，不能断言改善全部由limit120单独造成。本机本次连续性能通过，不推广为所有设备保证或绝对无泄漏证明。184次F6位置复位不是重赛；重赛另由前述比赛记录验收。

## 交付入口与最后核验

切换前生成复制清单，再保存当前候选、旧dist及入口脚本，共486个文件、300,558,931字节，逐项SHA256一致。快照在 `.local-releases/改造基线-0913/完整交付-0913/`，不覆盖旧归档。

实际运行新按钮时发现Windows启动网址中的 `&` 被cmd解释成命令分隔符。无浏览器的真实cmd解析实验复现：原参数只有 `art=anime` 留在网址，scope/quality被解析成命令。新增共享 `scripts/openLocalUrl.mjs`，只接受启动器生成的本机网址，使用显式引号与Windows原样参数；并保持短暂系统启动交接存活，避免复用服务器时Node提前退出。两个入口共同使用该修复。真实cmd回归确认完整查询参数不丢失，拒绝外站和命令字符；没有修改系统默认浏览器。

随后实际执行 `开始游戏.cmd` 打开独立Edge新标签，核对 `anime/high/full`、1920×1080、当前主脚本；正常键盘依次经过标题、模式、人机选人、有限血气CPU比赛、暂停、返回标题。比赛中无样板限制，人物为路飞/赤犬新图，音频ready；暂停playing=0，返回仍保留新画风。证据 `默认入口完整流程核验0913.json` 及两张同名前缀PNG。这是入口流程复核，不冒充新增整场录像。

最后 `typecheck`、`lint`、51文件564项测试与 `build` 通过，日志为 `正式交付-*.txt`（完整名称含0913）。验证构建写系统TEMP，主脚本仍为 `index-Crbo9vzy.js`；已验收的候选及旧dist未被构建覆盖。源码中战斗时长、伤害、范围未因本轮入口修复改变。

最终用户入口为 `开始游戏.cmd`，旧入口为 `开始旧版游戏.cmd`；`体验新版样板.cmd` 保留内部样板用途。游戏已具备完整流程与本机持续性能证据。最终混音听感、美术满意度及真人复玩仍分别保留；普通CPU面对路飞重复重拳的已知风险见 `CPU配对审查与回退0913.md`，没有用加血、加伤或提高防御概率掩盖。直接使用说明见 `完整游戏交付0913.md`。
