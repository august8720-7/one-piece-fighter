# CPU反击时机复核0913

**18:20配对审查结论：本页的时机优先候选已撤回，当前保留17:45之前的CPU行为。** 普通投优先存在可复现的选择问题；修正一次后，既定样本仍没有净收益，因此停止本轮策略迭代。下文保留17:55时的实验记录，不再表示当前接入状态。当前决定与复现材料见`docs/CPU配对审查与回退0913.md`。

状态：完成一项局部选择改进与固定策略前后复现；未解决普通赤犬面对路飞连续重拳的全部弱点，不据此声称游戏已经耐玩。记录时间为2026-09-13 17:55。

## 本轮结论

当前CPU源码核对为凌晨最终版，SHA256为`887f93375dda272bc433a8b813d6212160246ee010276b8890af322fa00c24e8`，复现的普通难度结果仍是玩家73/120胜，确认没有把旧的中间版本当基线。

发现的具体缺口是反击候选只检查可达距离，没有区分谁能赶上剩余收招。现改为：存在够得着、且按实际搓招耗时和启动帧能赶上的选择时优先选它；如果没有这类窗口，保留原有主动压制选项，不因此强迫CPU等待。保留原权重、反应间隔、选择概率和损招概率，不读取玩家即时按键。

最终固定策略结果为玩家77/120胜，较基线多4胜；CPU在玩家收招阶段的真实接触从72次增加到109次。它证明决策优先顺序和局部接触发生变化，不证明总体强度或趣味提高。普通赤犬面对路飞的结果完全未变，仍是路飞45/60胜；这个剩余问题继续保留，不以增加防御概率或血量掩盖。

## 可复现的接触边界

在真实模拟器里先让路飞重拳挥空，等待至动作第16帧（总29帧，剩13帧），再把地面双方放到100逻辑像素：

- 赤犬重拳能打到伸出的手臂，但12帧启动后首次接触时路飞已经收招；不能把“范围内”直接当成确反窗口。
- 冥狗虽然启动8帧，CPU实际输入还需要4帧搓招；真实输入序列执行13帧后，路飞已恢复idle，不能按8帧瞬发看待。
- 赤犬轻拳5帧启动，在路飞第21帧之后发生真实命中，确实处于收招中。
- 若路飞处于第15帧，多出的1帧足以让赤犬重拳在路飞第27帧之后接触。左右朝向均验证，不用一刀切禁掉长手反击。

这些探针只固定位置和策略意愿，没有改变招式数据。回归还检查已有12帧反应时钟、无窗口时主动压制的保留，以及未核验飞行时间的道具不能冒充直接打击确反。

## 为什么没有采用第一版过滤

第一版实验完全删除来不及完成确反的慢招，玩家胜场升至89/120，其中赤犬对普通路飞从28/60胜升至48/60。原因是“无法保证确反”不等于“此时不该攻击”：收招快结束时提前压制仍有价值，完全移除攻击会把CPU推向等待或前走。该版本没有作为最终策略采用，原始结果保留在`cpu-heavy-timing-after0913.json`，不能把文件名after误读为当前最终版本。

最终采用的是优先顺序修复，结果文件为`cpu-heavy-timing-priority0913.json`。这轮没有重新调整近、中、远分区，也未修改任何角色策略权重。

## 固定策略结果

沿用`docs/攻防复核0913.md`同一策略：脚点距离大于140时走近，否则每10帧单按一次C；99秒一局、先胜2局，有限血气，种子1至30，两角色与两玩家槽。120场全部到达match_end，没有超时上限或局内时间耗尽。CPU只从当前已完成的逻辑世界产生下一帧输入。

| 玩家角色 | 玩家槽 | 基线玩家胜 | 最终玩家胜 | 基线CPU收招接触 | 最终CPU收招接触 |
|---|---|---:|---:|---:|---:|
| 路飞 | P1 | 23/30 | 23/30 | 6 | 6 |
| 路飞 | P2 | 22/30 | 22/30 | 7 | 7 |
| 赤犬 | P1 | 13/30 | 15/30 | 30 | 49 |
| 赤犬 | P2 | 15/30 | 17/30 | 29 | 47 |

“CPU收招接触”为hit/throw/armor发生前玩家处于收招的计数，不全是专门反击分支产生。脚本另记的lateRecoveryActions是粗略启动下界诊断（忽略强化和接触边界），且包括普通中立分支选中的进攻，不能把它当作剩余错误数；精确的一帧边界以真实模拟器回归为依据。本轮未重复困难难度胜率统计。

## 文件、验证与边界

本轮源文件为`src/ai/cpu.ts`、`src/ai/attackRange.ts`，回归为`tests/ai/cpuTiming.test.ts`、`tests/ai/attackRange.test.ts`。新增的8项测试覆盖上述真实时间边界与策略优先级。

- `npx vitest run tests/ai`：5文件57项通过。
- `npm run typecheck`：通过。
- `npx eslint src/ai tests/ai`：通过。
- 本子任务不构建候选，不改中央素材清单、图片、角色数值、难度参数、core或浏览器；整库最终检查和桌面实玩由根任务统一记录。
- `src/ai/types.ts`仍为`f6c76786c52413f6ece827ab9d151bdf19cd34c8dc5a4177e68620dd1d422106`；当前`src/core/FightSim.ts`仍为`7d4331cd1c8f76bc5eeebbb2db3e04ea49fccf8b04da59fce6da0ad8bcb6bd46`。

资料复制清单：把临时审计脚本与改前两份源码分别复制为既有`.local-releases/改造验收-0913/`下的`cpu-timing-audit0913.mjs`、`cpu-timing-before0913.ts`和`cpu-range-before-timing0913.ts`，保留原件。三份比赛JSON、复制文件和最终源码的长度及SHA256记录在同目录`cpu-timing-hashes0913.json`。这些文件只作本地复现和回滚证据，不加载进游戏，也不覆盖凌晨证据。

剩余局限：时间筛选仍使用当前姿态的范围粗筛；它没有模拟玩家之后的移动、取消、无敌或阻挡，也没有预测飞行道具接触。因此名称为“赶得上的候选优先”，不是保证命中。不能把这次自动测试当作真人复玩、声音或精美程度验收。
