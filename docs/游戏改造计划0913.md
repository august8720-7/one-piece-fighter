# 海贼王格斗改造计划0913

状态：用户已批准连续实施，尚未完成。取代0912等待用户逐项样板批准的过程依赖；其他来源、完整性与真实性约束继续有效。

## 交付目标

完成两角色、一舞台的动漫格斗游戏，保留全部现有招式、人机、本地双人、训练、教学和完整比赛。依次保证：能完整玩、画面精美统一、攻防有趣且愿意反复玩。保留 Phaser、480×270逻辑世界、固定60Hz确定性核心，不扩展角色、联机、闯关或公开部署。

## 连续实施顺序

1. 保存旧版、样板与源文件的可核验基线。修复新人物加载失败自动回退、入口不一致、重赛丢失画风；基础资源损坏时不创建战斗，明确错误、重试及主动旧版入口。正常界面只显示小版本，详细信息按需诊断。
2. 并行修复音频错误缓存、实际加载计数、静音/零音量/未解锁/暂停/下载失败/解码失败/播放失败状态；提供开启、同路由试听、失败重试和主动恢复默认。保留用户音量，不改变系统设置。路飞伸缩重拳采用已有起手、挥出、回收三段采样，命中和格挡由真实事件触发。
3. 用已有原图重新打包，纹理密度与显示尺寸分离，根点、连接点一致变换。完成密度1/2与画布960×540/1920×1080四组本机对照。默认双倍密度+1920×1080，性能档双倍密度+960×540；单页上限4096并支持按动作组分页，记录实际内存。世界与伤害判定不变，界面及字体按目标画布重绘。
4. 补齐两角色所有可达基础状态和全部技能/强化形式。检查滑步、回收、近身接触、熔岩纹理；范围不能随人物画面漂移，不让拳头追踪对手，不改核心时长。已有原图能处理则先处理；需要新形体时只用核实的GPT Image 2.5官方网页。首稿及定向返工仍失败时调整方法并继续其他工作，不无限重复。
5. 新人物覆盖通过后解除样板强制训练、白名单、自动回血和绕过KO，接通标题、选人、有限血气、局胜、整场胜负、结算、重赛、重新选人。人物风格贯穿头像与界面；交战区域保留清楚的角色、血条、气槽、计时和必要提示。
6. 核验日语出力、受击、KO、胜利声音；无法确认招名时只用已确认的同角色通用出力。未听辨候选不得称为已完成。补充技能阶段和人物材质声音，避免密集叠加、重复喊声和暂停/退出残留。
7. 在完整比赛复核短按、搓招、取消、定格、跳落地、投技、墙角和掉焦恢复。固定种子及换边测试反复重拳策略，优先修CPU距离、防守后行动、惩罚和可达性，保留反应延迟，不读取即时玩家输入。冻结美术替换时的战斗数值；必要最小平衡变化单列前后对照。

## 全动作和技能覆盖

基础状态：站立、前后移动、蹲、跳起/空中/落地、冲刺/后撤、翻滚、站蹲空中拳脚、防御、受击、空中受击、倒地/起身、投/被投/拆投、KO与胜利。

赤犬：巨型熔岩拳、犬噛红莲犬头、流星火山三/八发轰击、地裂、冥狗、熔岩化。路飞：伸缩回弹、机关枪/暴风雨连续拳影、巨人拳、火拳铳、二档蒸汽、气球反弹；以实际招式表作为最终完整清单。

## 内部放行与最终验收

- 资源：正常、404、超时、非法配置、缺帧及恢复重试明确；请求新版不自动开始旧人物战斗。
- 声音：新设置、静音、音量零、启动失败、重试、暂停、试听、重赛、退出正确；实际事件、原始录音、浏览器音轨、用户实际听见分开记录。
- 画面：1280×720、1920×1080同显示尺寸检查；左右及近中远根点、接触、收招正确；每个可达状态与招式有合法素材。
- 完整游玩：两角分别通过标题→选人→人机→胜/负→结算→重赛；本地双人、训练、真实命中/格挡/连段教学转人机、设置返回可用。
- 工程：当前版本typecheck、lint、test、build全通过；冻结输入核心回放一致；独立桌面浏览器连续15分钟，记录实际显示帧间隔、逻辑60Hz、密集效果长帧与反复重赛资源变化。

内部检查通过后自动推进，无逐招用户批准环节。只有指定图像入口或用户必须处理的外部条件、既定红线需介入；局部阻塞不停止独立工作。不因整晚时间结束或测试通过把未完成项改为完成。

## 最终交付

默认完整新版的开始游戏.cmd、明确旧版入口和完整回退构建；同尺寸正常速度有声音前后对照及完整比赛记录；全动作/技能/声音覆盖、生成来源、本轮验收结果。用户醒后集中做人类听感与复玩评价。

## 决策记录

保留引擎，因为已确认问题集中在资源缩图/入口/音频状态/覆盖和体验，重写无法消除这些制作成本。继续使用指定网页图像模型以保证用户要求和来源可审计。取消中途用户样板批准以允许连续制作，但保留内部质量门槛，防止静图或覆盖计数掩盖动画问题。
