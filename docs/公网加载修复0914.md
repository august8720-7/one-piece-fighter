# 公网加载修复0914

状态：修复及本机慢网验证通过，准备更新原网址。仅处理跨电脑首次加载失败。

## 故障依据

- 用户失败电脑的加载诊断显示：marineford-backdrop、marineford-floor、fx-akainu、fx-luffy、路飞、赤犬全部请求超时。
- 2026-09-14 本机重新检查线上104份网页和资源，全部 HTTP 200 且 SHA256 与发布包一致。这证明当次本机可取得完整线上文件，不代表其他网络访问速度。
- 旧代码每角色整套资源共用8秒期限，舞台/FX各8秒，人物图片约32.6 MB。人物、舞台、70份短音同时请求，且图像使用 no-store，成功下载不能正常保留HTTP缓存。
- 判断：已确认失败原因是超过加载期限，尚不知道另一台电脑具体吞吐、路由或代理状态；不把它归为电脑性能或Windows激活问题。

## 最小修复与取舍

- 每文件开始实际下载后单独计时，90秒期限，网络中断/超时及临时HTTP错误自动重试一次；排队不扣下载时间，404/非法配置/解码与哈希错误不当成网络问题反复重试。
- 人物和舞台共享最多4个下载任务；图像完成后再预载声音，声音也最多4个任务同时下载。
- 短音单文件下载期限由4秒延长到30秒，保留已有失败提示与手动重试；音量、解锁和播放映射不变。
- HTTP缓存改为每次向服务器再验证，未变文件可以复用缓存，更新文件仍重新下载。原完整性、分页、解码与必需帧检查保留。
- 加载页显示当前阶段和真实完成文件数，重试时提示；失败页直接说明超时原因，诊断保留具体文件。不降画质、不换旧人物。
- 保留原构建与发布历史，完成加载相关实机与四项工程验证后更新原网址。本轮不重复15分钟战斗性能验收。

## 验收结果

- 类型检查、lint、build通过，54个测试文件586项测试通过；新增覆盖超8秒的实际响应体、排队时间不扣下载期限、并发上限、超时换新signal重试、持续失败的有限停止、HTTP/配置错误分类，以及图像与声音顺序。
- 同一本机内置Chromium浏览器、无缓存、150ms延迟、625000字节/秒（5 Mbps）：旧公开构建的两角人物和两角FX超时，停在Preload错误页；修复构建的全部18份人物/舞台/FX文件及70份短音成功下载，最后一份短音于导航后68.822秒完成，随后进入Title。赤犬主图单独下载50.779秒，路飞主图44.625秒；浏览器资源时间轴测得同时下载峰值4。该时间只代表指定模拟条件。
- 真实按键完成标题→菜单→双人选人→Fight，有限血气/计时、新人物和舞台正常，画面显示声音已开启。此次没有重做整场或15分钟验收，也没有把界面就绪当作真人听感。
- 实机临时阻断fx-luffy.png，正确停在错误页，诊断保留具体URL；解除阻断后点击“重新载入”，恢复Title，人物和舞台结果均ok。阻断与限速设置已清除。
- 缓存策略已改为标准HTTP再验证，但本次内置浏览器采集未确认实际304/磁盘命中，不把缓存提速计作已实测完成。
- 运行包仍为106份文件，47,411,590字节；相比上版只更新首页和游戏脚本，无人物/声音文件变化。脚本index-CyOZqboT.js的SHA256为06dd8e8cdd419b7eb8ebefa3b0d242f65ba2010fbf010db277eccf31d13abbca。

证据保存在本轮acceptance，旧公开包106份文件逐项校验备份在baseline，并保留源码Git bundle。Edge调试连接在本轮断开两次，随后使用内置浏览器完成上述验证，没有再要求用户重启浏览器或配合长时测试。

对方电脑的实际恢复需其重新打开新版确认，不能用本机模拟代替。若该网络持续无法获取GitHub文件，加长等待也不能代替网络连通性；本轮没有迁移托管平台。
