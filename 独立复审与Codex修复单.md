# 海贼王 2D 格斗游戏：独立复审与 Codex 优先修复单

复审对象：用户上传的《海贼王游戏复审0912.zip》。本文件是修复任务，不是已修改游戏的交付声明。

## 0. 实际读取、独立实验与边界

先读 `00_先读我.md`、`01_代码阅读入口0912.md` 和实际材料，形成独立结论后，最后读 `prior/` 三份旧计划/记录。以下源码路径均相对压缩包的 `project/`；证据图路径相对压缩包根目录。精确行号和原文见配套《源码证据摘录.md》。

### 已实际读取

- 入口及对照图：`evidence/01_user_clicked_new_link.png`、`02_anime_loaded.png`、`03_injected_pixel_fallback.png`、`04_daifunka_hit.png`、`05_luffy_video_timeline.png`，均打开像素查看。02 是历史成功图，03 是故障注入，不是用户本次现场。
- 原画：母版 `characters-master-0912.png`；两角 basics、格斗步法、路飞 reaction / short-recovery / stretch-repair、赤犬 daifunka-repair，以及 `marineford-main-0912.png`，共九张原图完成解码及全图/联系表目检。两角 atlas.png、basic_idle-candidate PNG 也已查看，并解析 atlas.json、runtime.json、构建报告和 anime_manifest.json。
- 代码重点：main.ts、index.html、screen.ts、BootScene / PreloadScene / FightScene / SettingsScene、assets.ts、FighterView.ts、FixedStep.ts、sampleMode.ts、InputHub.ts、AudioEngine.ts、audioCues.ts、Sfx.ts、sampleManifest.json、build_anime_atlas.py，以及相关测试文件的关键范围。未宣称逐行审完所有源码或运行全部测试。
- 音视频：51 个被 sampleManifest 引用的唯一 WAV 完成解码/非零信号检查；15.697 秒试听串烧、三段 MP4 完整解码并检查音轨。三段视频另作独立取帧目检：3.55 秒大喷火、3.10 秒路飞回收、2.35 秒路飞接触回收（最后一段视频流约 2.317 秒）。三段均为 960×540、60fps 视频流，含非零音轨。
- 来源与历史：人物生成记录、型号凭据、音频来源、浏览器记录与性能记录；最后才读 `prior/整体品质升级计划0912.md`、`整体品质实施记录0912.md`、`网页Pro规划建议0912.md`。

### 本次真正做成的独立检查

1. 以包内原始 basics 图、冻结裁切和原处理函数重建 idle：可见 RGBA 像素及 alpha 与已打包图完全相等。只在全透明区的隐藏 RGB 存在合成差异，不能说所有文件字节相同。
2. 同一原画以 2 倍纹理密度重处理，在相同显示大小作离线对照，面部、帽檐和衣服边线明显改善。没有重新生图，没有修改运行游戏；对照不是浏览器截图。
3. 将原音频 TypeScript 转译后执行独立控制流实验。AudioEngine 使用模拟 AudioContext/解码器，确证“master=0、HUD=ready、sampledPlays 仍增加”；直接执行 eventCues 确证 st_c 的 start/recover 为空。模拟节点不产声，不是听音或真实 WebAudio 验收。

### 没有完成的验证

没有正常速度主观观看或听辨这些视频/音频，不能评价具体音色、台词、混音力度和音画同步。没有运行成游戏：依赖安装遇到注册表 DNS 失败 EAI_AGAIN；独立浏览器测试页导航遭 ERR_BLOCKED_BY_ADMINISTRATOR，未绕过。没有本轮 typecheck/lint/Vitest/build 成功结果，不引用历史 333 项通过为本轮结果。没有访问用户本机的 127.0.0.1 页面，更没有确认其扬声器、浏览器标签页和系统混音器状态。

## 1. 核心判断

保留 Phaser + 固定 60Hz 战斗核心；保留角色设计母版、根点/连接点、显式帧停留和事件音频。必须改变“图集纹理像素与显示尺寸绑定”的生产规则、样板加载失败后的整对旧图回退行为、以及声音仅用 ready/播放计数判断成功的验收方法。

当前证据没有指向必须重写引擎。它指向两条可独立存在的显示问题，以及一条尚未取得现场状态的声音故障。原画并非全盘失败，也不能沿用现有缩放规则继续批量产图。

### 人物问题四种来源

| 来源 | 文件事实 | 判断 |
|---|---|---|
| 原画画成像素 | 母版和 basics 等原图是赛璐璐绘制；成功图与旧像素回退图明显不同 | 不支持“新原画本来就是旧像素画”。但每格源像素有限、姿势一致性与线条密度仍有上限 |
| 离线打包缩小 | prepare_frame 按 character.scale × source.unitScale 做 LANCZOS 缩小，idle 仅 158×183 / 175×212 | 已证实存在细节损失，是成功加载新版时仍显软/粗的重要原因 |
| 960×540 画布放大 | 逻辑 480×270；RENDER_SCALE=2；Phaser FIT；在更大显示区域会放大画布 | 已确认放大机制。当前用户实际 CSS 尺寸/DPR 未知，不能断言其放大倍率 |
| 失败后回旧图 | 任一角色载入失败或基础覆盖失败，resolveAnimeSample 整对换旧图；背景独立加载 | 已确认代码路径，且包内有受控故障截图。不能据此断言用户当次一定回退 |

入口截图支持用户确实点击“新版游戏样板”，不能反证最终资源成功。不要把排查起点设成“用户点错了旧启动文件”。新舞台正常出现，也不能证明新人物已加载。

### 已量化的密度损失

| 角色 idle | 当前实际原画来源 | 裁切宽×高 | 实际离线倍率 | 现有 atlas 帧 | 本次 2×密度重处理 |
|---|---|---:|---:|---:|---:|
| 路飞 | luffy-basics-0912.png | 305×354 | 0.5181065 | 158×183 | 316×367 |
| 赤犬 | akainu-basics-0912.png | 284×344 | 0.616 | 175×212 | 350×424 |

两角当前 idle 并非直接裁自那张大尺寸人物母版；“母版很高清”不能代表运行中的每一帧高清。图集整体 1024×2048 也不代表人物帧有这个分辨率。

`main.ts:21–23` 已经 antialias=true、pixelArt=false，anime 纹理在 `assets.ts:84` 和 `FighterView.ts:28` 已经 LINEAR。不要把“再打开抗锯齿”当主要修复。index.html 的 pixelated 默认样式仅在精确的 anime+training 入口条件下被 auto 覆盖，现场应检查最终 computedStyle，不能只看一个 CSS 声明。

## 2. 声音必须拆成三个问题

### A. 人物语音未完成：确定

sampleManifest 有 50 个 cue、51 个唯一 WAV，正式 voice.* 映射为 0。音频来源记录中的 36 个候选未听辨；这些候选原声文件不在本复审包内，不能据候选编号猜台词。已有 voice 分组与抢占机制可复用。这是内容及映射未交付，无法解释所有命中音、环境声也没有。

### B. 音效设计不足：有明确结构缺口，但听感未验收

路飞样板重拳 st_c 不在 MOVE_AUDIO；start 分支又只接受 sp_/ult_ 前缀。独立执行得到：start=[]，swing=[rubber_stretch]，recover=[]。rubber_stretch 绑定的是 stretch-start.wav；现成 stretch-release/end 文件没有作为该招三阶段使用。

因此必须同时修映射和 start 的前缀排除条件；只往 MOVE_AUDIO 加 st_c 不完整。赤犬大喷火的 start/release/end 映射已存在，不要全部推倒重写。音色是否薄、响度是否适合，仍需实际试听，不以非零波形替代。

### C. 整条播放链无声：现场原因未知，代码存在假就绪路径

AudioEngine 从 opf.audio.v1 读取持久化总音量/静音/分组音量。hudState 仅看 muted 与 context.running；主音量为零仍可 ready，SfxHudText 对 ready 返回空文字。playCue 没有将 master=0 作为停止或不可听状态，source.start 后仍计数。本次模拟节点控制流复现了这一组合，但不能推断用户现场音量就是零。

现有 InputHub 已在 keydown 和 pointerdown 调 unlock；不能只回复“点一下屏幕”。还需要区分 AudioContext 启动失败、文件/解码失败、应用暂停、总线音量、标签页静音、系统输出设备。load 对已有 error 直接返回，恢复网络后单纯再调 preload 未必会重试失败采样。

## 3. Codex 执行清单

### R0 / P0：让新版入口真实暴露人物状态，停止非自愿旧图回退

**文件依据**：assets.ts:57–91；FightScene.ts:65–110、278–290；main.ts:12–25；index.html:26–35。已有错误提示，但失败后仍在“新版样板”入口运行整对旧人物。

**改动**：
- 为候选构建显示短 buildId、requestedArt、两角 activeArt。诊断中记录最终入口、脚本/资产清单版本、纹理 key、加载/解析/覆盖/解码错误、canvas backing、CSS 尺寸、DPR 与 computed image-rendering。
- requestedArt=anime 但基础人物不可用时，暂停进入可玩样板，显示明确失败、重试、导出诊断。只允许用户主动选择“进入旧版”，旧版身份持续标示；不删除旧构建和旧素材。
- 保留严格覆盖校验。区分“基础资源坏了”与“基础可用但大喷火必需的附加状态缺失”；后者可保留基础新人物并明确禁用该招，不扩大到随机逐帧混用。
- 绑定入口到实际构建/资源清单，不只改链接文案。已有内容哈希纹理 key 和 no-store，保留；不要用到处加随机时间戳代替构建身份。

**可见变化**：用户不会在期待新版时不知情地操作旧像素人；错误明确且可恢复。成功页面只留小状态标识，不常驻大调试栏。

**最小验证**：同一个入口依次测试正常、PNG 404、runtime 错误、atlas 缺一帧、超时、只缺能力覆盖；正常纹理均为 anime 内容哈希 key；坏基础资源不得进入旧人物战斗。修复资源后重试确实重新取到对应资源。保持回退故障注入与用户现场证据分开保存。

**失败分支**：若加载成功而仍不清楚，转 R2；若资产哈希不符，修启动服务/构建一致性；若 crypto.subtle 不可用，显式报能力与安全上下文状态，不取消哈希校验。127.0.0.1 通常属于可信回环上下文，不可直接用“HTTP 不安全”解释所有失败。

### R1 / P0：建立可听确认，修复假就绪和不可重试

**文件依据**：AudioEngine.ts:79–123、142–181、208–253、332–363；Sfx.ts:7–9；InputHub.ts:20–25；FightScene.ts:412–414。

**改动**：
- 增加“开始并开启声音 / 试听 / 重试音频”，在本页真实手势中创建或恢复 AudioContext，并展示实际成功/失败。尊重保存的静音设置；经用户确认才恢复默认音量，不清空整个 localStorage。
- HUD/诊断区分未解锁、主动静音、总音量零、音效组零、应用暂停、下载/解码失败、引擎正在输出。记录原始 context.state、resume 异常、master/各组 gain、当前 cue、sample/synth 来源、丢弃原因、真实 pending 数，不以恒为 0 的 pending 表示任务结束。
- 试听使用现有短样本并经过同一 cue→group→master→destination 链。设置页/暂停页使用明确 ui:true 的试听允许规则，但不能绕过 master 或静音，以免制造“试听响、战斗无声”的误判。
- 在 master 前后加临时诊断电平，区分有事件、已调度、总线有信号、用户确认听见。引擎有信号不等于扬声器有声音。
- 显式重试只清理失败缓存项的 error/loading 等状态并重取，保留正常缓存；失败/设备状态变化保留诊断和可恢复入口。

**可见/可听变化**：第一次进入后能明确知道为什么无声；恢复后至少菜单/轻重击/格挡/环境声实际可听，不等语音制作完成。

**最小验证**：新配置、持久化 master=0、sfx=0、muted=true、resume 拒绝、单文件失败、暂停/恢复、设置页试听、重赛与退出。先导出原故障状态再改设置。真实浏览器回路录音与用户听见确认分开记录；正确状态下不双播、不补播暂停前积压声音。

**失败分支**：无事件→查 FightScene 事件；有事件但被丢弃→查明确原因；解码/启动失败→恢复链；master 后仍零→查总线；master 后有信号而人听不到→检查标签页静音、系统混音与输出设备。此时不要继续替换 WAV 或制作更多语音。

### R2 / P1：从原图重打高密度图集，同时修显示坐标换算

**文件依据**：build_anime_atlas.py:310–344、381–404；anime_manifest.json 的 scale/unitScale/basic_idle；screen.ts；main.ts；FighterView.ts:62–89；FightScene.ts:906–925。配套 density_experiment.json 和人物密度对照.png 是本次独立证据。

**前置补齐**：复审包缺少两个当前确实在用的源图：luffy-stretch-0912.png（stretch_windup）和 akainu-daifunka-0912.png（daifunka_release/guard）。先在原项目找回并核对冻结 SHA；包内文件缺失不表示用户运行时 PNG 已缺失，但会阻断完整再构建。不能拿返工图默默替代或谎称本包全量重建成功。

**改动**：
- 分离 textureDensity 与人物在游戏里的尺寸。先沿用已有原图为全部可达样板帧生成 D=2 候选图集，仍保存源图统一尺度、根点、袖口/肩/拳连接点；不得每帧按包围盒独立缩放到同高。
- 做 2×2 控制实验：原/2×纹理密度 × 960×540/1920×1080 backing。比较时使用同 viewport、DPR、人物显示高度、姿势和逻辑状态；不能把人物放大冒充变清楚。
- 若 backing 方案通过，建立明确的逻辑→渲染换算，保留 core 的 480×270、60Hz 和全部判定。当前动漫 sprite scale 被锁为 1，不能只将 RENDER_SCALE 改成 4。
- 一个可审查的换算约定：B 为每逻辑单位的渲染像素数（当前 2），D 为相对现有图集的密度；在根点/连接点同步按 D 重建的前提下，人物 spriteScale=(B/2)/D。B=2,D=2 时 0.5；B=4,D=2 时 1。背景、HUD、固定像素边距与特效使用同一视图换算；不要直接拉伸已栅格化旧文字。
- LINEAR/非像素设置保留。选择实际尺寸与 DPR 下足够的 backing，性能不足提供经验证的画质档，不无限 DPR 倍增、不突然退回像素图。

**可见变化**：保持人物大小、站位、攻击距离不变，脸、帽檐、红衣边线和手指更清晰；全屏不再只是放大旧低分辨率画布。

**最小验证**：1280×720 与 1920×1080 显示，记录真实 DPR；静止同帧对照加走动/挥拳/受击/镜像/最大伸臂。绘制根点与 sockets，仅调试模式显示；检查显示身高、接地和命中前缘不漂移。冻结相同代码/招式/种子和合法输入做 core 回放比较，另行测浏览器帧时间与内存。

**失败分支**：D2 原画仍糊→检查该姿势源像素，才局部重画；D2 静图清楚而页面仍糊→检查 backing/CSS/DPR/computedStyle/纹理实际 key；出现身体缩小或拳点漂移→修坐标转换，不改判定；显存超限→根据测量分图集或设画质档。两张现有 1024×2048 RGBA8 基础纹理合计约 16MiB，只是基础像素存储，非总显存；加密度须重新测量。

### R3 / P1：先接好现有路飞三段音，不再堆同类素材

**文件依据**：audioCues.ts:39–53、103–120；sampleManifest.json:82–120、550–558；FightScene 的真实 start/swing/recover/hit/block 事件。配套 independent_audio_logic_checks.json 记录原映射输出。

**改动**：显式将 st_c 接入 stretch profile，同时调整 start 前缀守卫，使已声明普通招 profile 不被 sp_/ult_ 限制丢弃。start / swing / recover 分别使用现有 stretch.start / release / end。保留真实 hit/block 的接触反馈及空挥差异，不用动画播放到某张就假造命中音；不重复播放旧 rubber_stretch 别名。声音跟随招式实例和事件，受击中断、取消、重置不得补播收尾。

**可听变化**：拉伸起手、发力、回收可区分；击中/格挡与空挥有不同反馈。不是把所有音量推满。

**最小验证**：st_c 空挥、命中、格挡、受击打断、暂停重赛，两朝向；每个阶段在真实事件处只播一次。赤犬大喷火三段回归，多段招不误消音或堆叠长尾。离线表和日志确认事件，真人 A/B 试听确认混音力度和时序。

**失败分支**：日志正确却无声→R1；均可听却仍薄→再做短冲击/主体材质/尾音的少量混音候选，由实际听辨决定，不靠文件数宣称提升。

### R4 / P2：接入少量已经听辨的角色语音，不把候选当完成

**文件依据**：sampleManifest 无 voice.*；docs/assets/音频来源0912.md:5–11、13–26、132 之后；AudioEngine 的 voice 分组/角色抢占；audioCues.ts:99–113、122–131。

**改动**：先找回候选声音，实际听辨两角的通用出力、受击、KO；样板招名只有确实听到对应内容才映射，否则用已核实的同角色出力。记录来源、截取起止、角色/语义、底噪/伴奏、采用状态、响度和用途边界，接入正确 voice.* 键。每个招式实例一次，受击有冷却，KO 优先，不打断成一句未完又一句。样板未开放的 KO/胜利不得为听音强行开放；在试听页或完整模式验收该内容。

**可听变化**：当前样板至少听到路飞和赤犬真实、语义合适的出力与受击反馈，角色感建立。

**最小验证**：人实际听原片与切片并确认；两角不串音；攻击、受击、KO 抢占正确；语音组单独静音不影响 SFX。来源记录称再分发许可未确立，不把候选资料当授权证明。

**失败分支**：没有合适/可用的原声则保留明确未完成项，继续可听 SFX 样板，不拿合成哔声或 TTS 冒充角色原声，也不把缺语音解释成整体无声。

### R5 / P2：仅精修现有动作闭环与同屏主次，不扩全招表

**文件依据**：runtime.json 两角 idle 都是一帧、walk 四帧；两角 atlas、原画、evidence/02/04/05 与本次取帧图；build_anime_atlas 中的局部变形。最后读到的实施记录:19–23、34–38 也承认近身穿过、跟步滑行及熔岩纹理拉長，尚未主观放行。

**改动**：
- 保留“母版 + 关键姿势 + 根点/连接点 + 逻辑曝光”的方法，减少拥挤整页、多次独立全身图造成的低有效分辨率与漂移。只有现有原图确实缺形体时，才按一个动作/相邻少量姿势作定向修图。
- 先精修待机→前后跟步→轻拳→伸臂/大喷火→受击/倒地/起身的已有闭环。可试受控局部呼吸/衣摆，脚与躯干比例不变；不把整人上下压缩当呼吸，不机械增加原画数量。
- 直橡胶段与有限熔岩中段配准可保留，但保护肩、袖、身体与拳形；长段被拉成均匀纹理、肘部转向或遮挡变化需要专门姿势/局部绘制，不继续加大整图变形。
- 贴身、中距、最远端分别验证攻击主体与实际接触点。如果核心允许整段攻击臂命中，视觉须让接触发生的部位可读；不要擅自把大喷火改成飞行拳、让手臂追踪对手或把判定改到远端只为画面成立。
- 同屏只做必要修正：收起当前顶部大说明与底部次要训练提示，保留血条/计时/关键键位；在渲染层减弱交战带纹理干扰、补克制接地关系。先用现图参数化比较，不先重画整张舞台，也不靠大光效遮住动作问题。

**可见变化**：清楚的动漫人物不再只是孤立姿势切换；脚下滑动、近身穿透和岩浆长纹理问题更容易被真实修正；战斗主次更明确。

**最小验证**：真实键盘正常速度操作，左右朝向和三个距离，空挥/命中/格挡/中断/墙角/定格后恢复。保存按键→逻辑接受→首个动作画面→接触→恢复行动的时序；如果存在跟手问题，用该日志定位输入/呈现，不偷改招式硬直。状态/事件回放不变只是工程门槛，还需正常速度人工看、听、操作认可。

**失败分支**：同一难点定向返工仍失败，停止该机制扩产，保留对照，改部件划分或动作制作方法；不能靠更多粒子、无限补图或整套换引擎回避。复玩性仍未验证，不能因训练样板几招连贯就宣称达到拳皇手感或耐玩。

## 4. GPT Image 2.5 官方网页约束

本次没有生成新 AI 图像。密度对照仅是已有源图的确定性重处理。

独立在线核对了 OpenAI 的 ChatGPT Images 2.5 公告、官方帮助页及 chatgpt.com/images 公共入口。公告说明网页端可用，入口页面标题显示 ChatGPT Images 2.5。没有验证用户当时每个登录会话、每张图的后台模型，不能把生成记录文本的哈希当成模型来源证明。

Codex 对所有新增 AI 人物、动作、背景、特效继续执行：生成前核对官方域名和该次 Images 2.5 入口/页面型号证据，保存可读页面记录、提示词、参考图 SHA、时间、会话与原始 PNG SHA；确定性裁切/抠底/重采样另存父子文件关系。页面无法核实就停该次生图。不得用规划模型 6 Pro、提示词写的型号、API、第三方生图站或其他模型代替官方网页约束。

外部核对入口（外部依据，不用于证明附件各个历史生成会话）：
- https://openai.com/index/introducing-chatgpt-images-2-5/
- https://help.openai.com/en/articles/11084440-chatgpt-images-faq
- https://chatgpt.com/images/
- Phaser FIT 与画布/CSS 区分：https://docs.phaser.io/api-documentation/class/scale-scalemanager
- WebAudio 手势与用户控制：https://developer.mozilla.org/en-US/docs/Web/API/Web_Audio_API/Best_practices
- 本地可信上下文：https://developer.mozilla.org/en-US/docs/Web/Security/Defenses/Secure_Contexts

## 5. 对最后读到的旧计划的取舍

不重写“两个机制小样→基础攻防→完整比赛”的宏观路线。旧计划已经明确固定核心、先验收再扩量、声音和主观观感未完成，不能把它歪曲成已经承诺全部验收。新增的、必须改变执行的点是：把高清要求落到具体源帧→atlas→backing 像素预算；把加载失败后的完整旧图回退从默认可玩行为改为明确阻断/主动选择；把“已有音频通路”升级为本次实际可听证据；补齐 st_c 的真实事件映射，而不是再下载一批音频。

保留的底座值得继续，不等于保留当前制作参数和验收方法。上述 R0–R3 成果应先在用户实际入口中出现，再决定更多人物原画、更多招式或更大 UI 改造。

## 6. 本轮交付和最后一个关键现场证据

Codex 交付应为同一个可识别的新样板入口、R0–R5 分项状态、只包含本轮实际执行结果的回归记录，以及同显示尺寸/同动作的前后对照。缺依赖、没听音、没真人操作、没做 15 分钟运行时都单独标未验，不沿用旧成功记录。不得在当前有限样板上宣称完整比赛、真人复玩或手柄已验收。

最关键缺项只有一份：**用户实际出现问题的同一页面、在重置设置前，完成一次约 20–30 秒的带声音诊断现场采集。** 同次采集保存入口与 buildId、两角真实 texture key/加载错误、canvas/CSS/DPR、master/分组音量/context/丢弃原因/master 后电平、轻拳和两招操作，以及系统音频录制与“本人是否听见”的确认。可由一个“导出复审现场”按钮连同短录屏形成一个包。先采原状态再恢复；不要让清缓存、重置音量或换入口消掉最有价值的证据。
